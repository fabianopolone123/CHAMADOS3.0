# 06 - Changelog

## 2026-07-29

- Kaspersky: **os chips de filtro sairam das duas abas** (situacao e setor na aba Dispositivos; situacao na aba Colaboradores) e ficou **apenas o campo de pesquisa**, que agora acha tudo. Alem dos dados (nome, setor, responsavel, colaborador vinculado, IP, status, versoes, grupo, observacoes e ultima conexao), o texto pesquisavel de cada linha leva as **palavras das situacoes**: digitar "critico", "sem antivirus", "sem conexao", "fora do export" - ou, na aba Colaboradores, "com antivirus"/"sem dispositivo" - filtra a lista como os chips faziam, com a vantagem de combinar com qualquer outro termo (varias palavras, todas precisam bater; ignora acentos; `Esc` limpa). Conferido no navegador com os dados reais: "critico" -> 7, "sem antivirus" -> 13, "projetos" -> 9, "192.168.22" -> 23 dispositivos. Removida a contagem por setor que alimentava os chips (`setores` no contexto) e a logica de chips em `static/js/kaspersky.js`.
- Kaspersky: **corrigida a aba Colaboradores, que mostrava 49 pessoas "Sem dispositivo"** enquanto o modulo Contatos mostrava o computador delas. A aba olhava so o vinculo direto `KasperskyDispositivo.ramal` e ignorava o **inventario do GLPI**. Agora quem tem computador no GLPI que o Kaspersky nao conhece aparece como **"Sem antivirus"**, com a maquina marcada "(so no GLPI)" - que e a informacao importante: **computador sem o antivirus gerenciado**, antes escondido no meio de quem nao tem maquina. Com os dados de producao, os 49 "sem dispositivo" viraram **24 sem antivirus** (tem computador no GLPI, nada no Kaspersky, ex.: Adrian Aleixo -> CPU-146) e **25 sem maquina** de fato; no total, 28 protegidos / 31 sem antivirus / 25 sem maquina. O cartao de resumo passou a mostrar as duas contagens ("31 sem antivirus - 25 sem maquina") e ha o novo contexto `colaboradores_sem_antivirus`. Testes `KasperskyColaboradoresTests` (5 casos, incluindo o computador que existe nos dois lados e nao deve duplicar).
- Corrigido um comentario de template que **aparecia escrito na tela** do Kaspersky: `{# ... #}` do Django e comentario de **uma linha** e o meu tinha ficado em duas, entao o Django o tratava como texto normal e o renderizava (um deles estava dentro da tag `<tr>`, virando lixo no HTML). Os tres foram corrigidos e ficou o teste `TemplatesLintTests`, que varre `templates/` e falha se algum `{# #}` estiver quebrado em varias linhas. Detalhe que atrasou o diagnostico: o Django 4.1+ usa o **cached template loader mesmo com `DEBUG=True`**, entao o servidor de teste continuava servindo o template antigo ate ser reiniciado.
- Planilha de atendimentos: **corrigido o bloco de resumo e a coluna Tempo vindo em branco**. O modelo traz `=SUM(F2:F5)` no total e `COUNTIF` na contagem por prioridade, e a coluna Tempo saia como `=Fechado-Data`, mas o openpyxl grava formula **sem valor em cache** (`<f>...</f><v/>`): ao abrir, o Excel mostrava essas celulas vazias e o grafico do resumo lia zero - mesmo com o `fullCalcOnLoad` ligado no arquivo. Agora os valores vao **calculados** (total de linhas, contagem de "Baixa"/"Programada" e a duracao de cada periodo), sem nenhuma formula: a planilha abre certa em Excel, LibreOffice ou Sheets, sem depender de recalculo. Em troca, editar uma linha a mao nao atualiza mais o Tempo nem os contadores sozinho. O Tempo passou a usar o formato **`[h]:mm:ss`** (tempo decorrido) em vez do `h:mm:ss AM/PM` do modelo, porque ha 14 atendimentos de mais de 24 horas na base (o maior com 7 dias, de um Play esquecido aberto) e no formato antigo 169 horas apareceriam como "1:37:03 AM". Novo helper `_preencher_resumo` e constante `FORMATO_DURACAO` em `core/planilha_atendimentos.py`; testes novos em `PlanilhaAtendimentosTests` (resumo calculado e atendimento de mais de 24h).
- Chamados: **importados os 886 periodos de atendimento que ficaram no sistema antigo**. A migracao original trouxe os 757 chamados e as 3978 mensagens, mas nao a tabela `chamados_ticketattendance` do banco antigo, onde estava o tempo trabalhado - por isso a planilha mensal e a tela de Historico so tinham dados de 15/07/2026 em diante e os meses anteriores saiam em branco. Agora fev-jun/2026 aparecem: 02 -> 69, 03 -> 145, 04 -> 116, 05 -> 234, 06 -> 216 periodos, distribuidos entre fabiano.polone (469), fabio.generoso (384) e marcelo.sorigotti (38). Novo `core/importa_atendimentos_legado.py` + migration de dados `0048`, que le `seed/chamados_legado.sqlite3` (banco antigo, **nao versionado**; sem o arquivo a migration e no-op). Mapeamento ticket antigo `id` N -> `CH-{N:06d}` (faixas nao se cruzam: migrados CH-000003..CH-000799, novos a partir de CH-000800), com a data de criacao do chamado conferida registro a registro. **Protecoes** para nao bagunçar nada em producao: nunca grava periodo sem fim (viraria "atendimento ativo", sujando os contadores do Kanban e bloqueando o Play do atendente - os 5 Plays abandonados no dia da virada foram descartados); ignora periodo que comece a partir do primeiro periodo do sistema novo (anti-duplicidade, calculado ignorando os proprios registros legados para que uma importacao interrompida ainda possa terminar); idempotente pela chave (chamado, atendente, inicio); e **nao altera nenhum campo de `Chamado`**. Ensaiado numa copia do banco de producao antes de subir: 886 criados, chamados 100% inalterados (status, encerrados e `atendente_atual` iguais) e nenhum atendimento ativo novo. **Fuso:** o banco antigo grava naive em **UTC** - conferido pelo periodo de 04/05 (11:48->17:39 no banco, 08:48->14:39 na planilha preenchida a mao) e pela hora de abertura dos tickets, concentrada entre 11h e 21h (= 08h-18h local). Testes `ImportaAtendimentosLegadoTests` (11 casos, com um banco legado sintetico cobrindo cada protecao).
- Planilha de atendimentos: duas colunas passaram a seguir exatamente a planilha preenchida a mao, conferida linha por linha contra `05-2026 - Fabiano.xlsx`. **Notificacao** leva a **descricao** do chamado - o pedido como o usuario escreveu, para os chamados migrados e para os novos (ex.: "Impressora esta apresentando faixas nos desenhos..." em vez de "RICOH"), caindo para o titulo quando nao houver descricao e removendo os metadados que a migracao anexou no fim do texto ("Tipo legado: ... | Falha legado: ...", `[ERP-TI-ID:n]`). **Prioridade** passou a ser apenas **"Programada"** (trabalho da propria TI: Kanban, pendencia, solicitante TI/admin ou o tipo "programado" do sistema antigo) ou **"Baixa"** para demanda de usuario, **independente da prioridade gravada no chamado** - na planilha a mao, chamados que o sistema marca como "alta"/"critica" aparecem como "Baixa", porque prioridade e controle interno. Ajustes em `core/planilha_atendimentos.py` (`_notificacao`, `PRIORIDADE_TI`/`PRIORIDADE_USUARIO`).
- Chamados/planilha: o seletor de mes do modal passou a oferecer **apenas os meses em que aquele atendente tem atendimento registrado** (com a contagem no rotulo, ex.: "Julho 2026 (23 atendimentos)"), mais o mes atual, que entra sempre. Antes eram 12 meses fixos, o que levava a baixar planilhas em branco de meses anteriores ao inicio do controle de tempo - o sistema so tem periodos Play/Stop desde que essa etapa entrou em uso, e os meses antigos foram preenchidos a mao fora dele. Quando o atendente nao tem nenhum atendimento no periodo, o modal avisa que a planilha sairia so com o cabecalho. A lista sai de um `TruncMonth` agrupado por atendente (`_meses_com_atendimento` em `core/views.py`, no lugar do antigo `_opcoes_meses_planilha`) e vai para o botao de cada coluna em `data-planilha-meses` (JSON), com o `select` montado no cliente (`initializePlanilhaDownload` em `static/js/chamados.js`). Adicionado tambem `scripts/diagnostico_planilha.py` (somente leitura) para conferir no servidor de onde a planilha consegue tirar linhas: total de chamados e encerrados por mes, periodos de atendimento por mes e por atendente, e quantos encerrados nao tem nenhum periodo (esses nao aparecem na planilha hoje). Testes novos em `PlanilhaAtendimentosTests` (meses com atendimento e atendente sem atendimento).
- Chamados: cada coluna de **Atendente TI** no Kanban ganhou um botao para **baixar a planilha mensal de atendimentos** (.xlsx). O botao abre um modal que pergunta o **mes** (os 12 ultimos, com o **atual como padrao**) e baixa o arquivo daquele atendente - acabando com o preenchimento a mao. A planilha sai no **mesmo modelo que a TI ja usava** (`core/planilhas/modelo_atendimentos.xlsx`, versionado no projeto): mesmas colunas, cores, larguras, filtro do cabecalho e as formulas de resumo por prioridade; nome do arquivo `MM-AAAA - <PrimeiroNome>.xlsx`, aba com o nome do mes ("Julho"), `A4` = "Atendimentos TI Sidertec - MM/AAAA" e `A5` = nome do atendente + telefone puxado dos **Ramais**. A regra central e **uma linha por atendimento, nao por chamado**: cada Play -> Pause/Stop (`AtendimentoHistorico`) vira uma linha, entao um chamado trabalhado em tres dias aparece tres vezes, como era feito a mao. **Data** = hora do Play, **Fechado** = hora do Pause/Stop, **Tempo** = formula `=Fechado-Data`, **Contato** = solicitante, **Setor** = setor do solicitante casado com a lista de Ramais (pelo e-mail e, se nao achar, pelo nome, reaproveitando o casamento por conjunto de palavras do modulo Contatos; em branco quando nao ha match), **Notificacao** = titulo do chamado, **Prioridade** = "Programada" quando o chamado nasceu na propria TI (Kanban, pendencia ou solicitante TI/admin) e senao Baixa/Media/Alta (`critica` entra como Alta), **Falha** = "N/A"; **Tk** e **Acao Eficaz** ficam vazias, como nas planilhas atuais. O mes e recortado pelo **inicio** do periodo (um atendimento que comeca dia 31 e termina dia 1 fica no mes em que comecou); periodo com Play ainda aberto sai com Fechado/Tempo em branco; e **chamado encerrado direto pelo Stop (sem Play) nao gera linha**, por nao criar periodo de atendimento. As datas saem como data/hora reais (`dd/mm/yyyy hh:mm`), nao texto, para a formula de tempo e a ordenacao funcionarem no Excel. **Qualquer Atendente TI/admin baixa a planilha de qualquer atendente** - diferente da tela de Historico, que restringe o atendente aos proprios registros (`_get_history_queryset_for_user`, conferido: a regra existe e esta ativa; quem e admin ve tudo). Novos arquivos `core/planilha_atendimentos.py`, `core/planilhas/modelo_atendimentos.xlsx` e `templates/partials/planilha_atendimentos_modal.html`; nova rota `GET /chamados/planilha/<attendant_id>/?mes=AAAA-MM` (name `atendimentos_planilha`, `400` para mes invalido e `404` para quem nao e atendente); ajustes em `core/views.py` (`atendimentos_planilha_view`, `_setor_por_solicitante`, `_telefone_do_atendente`, `_opcoes_meses_planilha`), `core/urls.py`, `templates/chamados/dashboard_atendente.html`, `static/js/chamados.js` (`initializePlanilhaDownload`) e `static/css/chamados.css` (`.column-sheet-btn`). Adicionado **openpyxl** ao `requirements.txt` (o deploy roda `pip install -r requirements.txt`). Sem model nem migration. Testes `PlanilhaAtendimentosTests` (12 casos) e fluxo conferido no navegador com Playwright: botao na coluna, modal com o mes atual, download real do arquivo e conferencia do conteudo linha por linha (inclusive um chamado com tres periodos em dias diferentes).
- Kanban: corrigido o **Stop de chamado em "aguardando"** (usuario/peca/autorizacao), que mostrava o botao mas ao clicar respondia **"Este chamado nao possui atendimento ativo para voce."** e nao abria o modal de encerramento — na pratica o encerramento direto (regra 10a), liberado no backend em 27/07, nunca funcionava pela tela. A causa era uma guarda antiga em `bindActionButton` (`static/js/chamados.js`) que barrava **qualquer** acao de card sem Play ativo antes de chamar o modal; ela nao foi atualizada quando o encerramento direto foi criado, embora o proprio arquivo ja tivesse o helper `isDirectClose` (usado so para exibir o aviso dentro do modal) e o backend ja aceitasse o Stop. Agora a guarda abre excecao para `action === "stop"` quando `isDirectClose(card)` e verdadeiro (sem Play + status de "aguardando"), espelhando a regra do `finish_attendance_view`; o Pause sem Play continua bloqueado, e chamado sem Play fora de "aguardando" tambem. Correcao **so no frontend** (sem model, rota, migration ou mudanca de backend). Reproduzido e validado no navegador com Playwright: com o codigo antigo o toast de aviso aparecia e o modal nao abria; com a correcao o modal abre com o aviso "sera encerrado sem atendimento ativo", o chamado fecha ("encerrado e movido para Fechado"), o card sai da coluna e a linha do tempo recebe a mudanca de status + o evento `encerramento_direto`, sem criar periodo em `AtendimentoHistorico`. Os 24 testes de `EncerramentoChamadoTests` seguem passando (o backend ja estava correto).
- Requisicoes: o **"Copiar p/ e-mail" nao leva mais o rodape** "Portal de TI - requisicoes de compra" — o conteudo copiado termina no total do ultimo orcamento. Removida a linha do rodape e o estilo `rodape` (agora sem uso) do `EMAIL_STYLES` em `static/js/contratos.js`; a versao em texto puro nunca tinha rodape. Conferido no navegador lendo a area de transferencia (HTML sem o rodape, sem erro de JS).
- Requisicoes: a lista ganhou uma **pesquisa inteligente** no topo, que filtra **a cada tecla digitada** e acha **qualquer coisa da requisicao** — nao apenas o codigo/titulo que aparecem na linha. O texto pesquisavel de cada requisicao leva codigo, titulo, tipo, status, descricao, quem criou, data de criacao, quem entregou e **todos os orcamentos e suborcamentos** (titulo, loja, link, valor unitario e total formatado) mais os **nomes dos documentos anexados**; orcamento aprovado tambem responde por "aprovado". Assim da para achar uma requisicao pela loja ("Kabum"), pelo produto de um complemento ("mouse"), pelo valor ("9.800,00") ou pelo anexo, sem abrir uma por uma. A busca **ignora acentos e maiusculas** ("LICENÇA" acha "Licenca") e aceita **varias palavras**, todas precisando bater em qualquer ordem ("notebook rh"); abaixo do campo ha o contador "X de Y requisicao(oes) encontrada(s)", sem resultado aparece "Nenhuma requisicao encontrada para esta pesquisa." e `Esc` limpa a pesquisa. O filtro roda **no navegador** (resposta instantanea, sem rota nova e sem ida ao servidor) sobre o `data-search` que a view monta no novo helper `_requisicao_busca_texto` (`core/views.py`), com o `contratos_dashboard_view` passando a usar `select_related`/`prefetch_related` dos orcamentos, suborcamentos e documentos para nao gerar N+1. Para a busca nao ficar velha durante a sessao, o texto pesquisavel e **reconstruido no cliente** (`buildRequisicaoBusca`, espelhando o backend) sempre que o detalhe da requisicao e carregado — cobrindo novo orcamento, troca de loja, edicao e mudanca de status sem recarregar a pagina — e tambem ao criar/excluir uma requisicao da lista. Ajustes em `templates/chamados/contratos.html` (campo de busca, contador, aviso de vazio e `data-requisicao-row`/`data-search` no item), `static/js/contratos.js` (`normalizeSearch`, `applyRequisicaoFilter`, `setRequisicaoBusca`, `buildRequisicaoBusca` + ganchos em `addRequisicaoToList`, `loadRequisicaoDetail` e `removeRequisicaoFromList`) e `static/css/contratos.css` (`.requisicoes-search*`, `.requisicoes-no-results`, no mesmo padrao visual do modulo IPs). Sem model, migration ou rota nova. Testes `RequisicaoBuscaTests` (5 casos) e fluxo conferido no navegador com Playwright (busca por loja, suborcamento, descricao, codigo, duas palavras, acento, valor formatado, sem resultado, `Esc` e apos abrir o detalhe), sem erro de JS.

## 2026-07-27

- Kaspersky: os dispositivos agora **mostram o nome do colaborador e o setor sem digitar nada**, puxados do inventario do GLPI (modulo Contatos) pelo **nome da maquina** — a nomenclatura e a mesma nos dois sistemas. O vinculo roda nas duas importacoes (tanto ao importar o export do Kaspersky quanto ao importar o CSV do GLPI, em qualquer ordem) e tambem no novo botao **"Puxar nomes do GLPI"** no topo da tela, util quando os dois arquivos ja tinham sido importados antes. So preenche o que esta em branco e **nunca troca um vinculo feito a mao**; o toast informa quantos dispositivos receberam o nome. Com os arquivos reais, 17 dos 25 dispositivos passaram a mostrar responsavel e setor (os 8 restantes sao servidores, que nao tem usuario), e os chips de setor no Kaspersky passaram a listar Compras, Contratos, Financeiro, RH, TI etc. Novo helper `_vincular_kaspersky_pelo_glpi` em `core/views.py`, nova rota `POST /kaspersky/sincronizar-glpi/` (name `kaspersky_sincronizar_glpi`) e 4 testes novos em `ContatosTests`.
- Adicionado o modulo **"Contatos"** (menu lateral, apenas TI/admin) em `/contatos/`: uma **lista unica por pessoa** juntando **colaborador, setor, computador (o "nome do CPU"), e-mail, ramal, telefone e antivirus** — os modulos passam a conversar entre si. Os computadores vem do **CSV exportado do GLPI** (botao "Importar GLPI"; upsert pelo nome do computador, aceita `;`, `,` ou TAB) e cada maquina e ligada ao colaborador **pelo nome do usuario do GLPI**, comparando o conjunto de palavras sem acento — resolve a inversao ("Gabriele Ana" x "Ana Gabriele") e nomes do meio a mais ("Garbuio Tamara Cristiane" x "Tamara Garbuio"), so aceitando quando ha um unico candidato. No arquivo real, **60 dos 83 computadores** foram vinculados sozinhos; o resto e ajustado a mao clicando no computador (e esse ajuste **nunca e desfeito** por uma importacao seguinte). O **e-mail/ramal/telefone** vem do modulo Ramais e o **antivirus** vem do Kaspersky, cruzado pelo nome do computador (mesma nomenclatura CPU-010/NOT-362), mostrando "Protegido" ou "Sem antivirus" por pessoa. Quem esta no GLPI mas nao tem ramal aparece com a etiqueta "so no GLPI"; maquinas sem usuario no GLPI nao viram contatos falsos (ficam agrupadas em uma linha "(maquinas sem usuario no GLPI)"). A tela tem cartoes de resumo (contatos, com computador, com e-mail, sem computador), busca e filtros por situacao e por setor, e um modal com os dados do GLPI (usuario, tipo, fabricante, modelo, processador, sistema, localizacao, status, antivirus e data) + troca do colaborador vinculado. Model `Computador` (FK opcional para `Ramal`, migration `0047`), registrado no admin; rotas `GET /contatos/`, `POST /contatos/importar/` e `POST /contatos/computadores/<id>/`. Novos arquivos `templates/chamados/contatos.html`, `partials/contato_modal.html`, `static/css/contatos.css` e `static/js/contatos.js` (nao confundir com `contratos.*`, do modulo Requisicoes). Testes `ContatosTests` (11 casos) e fluxo conferido no navegador com o CSV real do GLPI + o export do Kaspersky.
- Adicionado o modulo **"Kaspersky"** (menu lateral, apenas TI/admin): controle dos dispositivos com antivirus e das licencas. A lista e atualizada enviando o arquivo do botao **"Exportar"** do portal (`export.txt`, TSV com Nome, ultima conexao, agente, status, versoes, IP e grupo); a importacao e um **upsert pelo nome** (atualiza os existentes, cria os novos) e **preserva setor, responsavel, observacoes e o colaborador vinculado**, que sao dados nossos. Quem nao vem no arquivo nao e apagado: fica marcado como "fora do export", esmaecido, e deixa de consumir licenca. **Licencas**: quantidade contratada configuravel (padrao 100, clicando no cartao) e consumo calculado pelos dispositivos do ultimo export com o antivirus instalado — maquina so com o Agente de Rede nao conta; os cartoes mostram contratadas / em uso (com barra e percentual) / disponiveis / dispositivos cadastrados. A tela tem busca, filtros por situacao (Criticos, Sem antivirus, Sem conexao ha 7+ dias, Fora do export) e por **setor**, tabela responsiva com badges e ordenacao por cabecalho, e modal para editar setor/responsavel/observacoes (com sugestao de setores vindos dos Ramais). Segunda aba **"Colaboradores"** cruza a **lista de ramais** com os dispositivos e mostra quem esta **Com antivirus**, **Sem antivirus** ou **Sem dispositivo**, com busca e filtro (quem precisa de atencao primeiro). Models `KasperskyDispositivo` (com FK opcional para `Ramal`) e `KasperskyConfig` (migration `0046`), registrados no admin; rotas `GET /kaspersky/`, `POST /kaspersky/importar/`, `POST /kaspersky/licencas/`, `POST /kaspersky/<id>/editar/` e `POST /kaspersky/<id>/excluir/`. Novos arquivos `core/views.py` (bloco Kaspersky), `templates/chamados/kaspersky.html`, `partials/kaspersky_modal.html`, `static/css/kaspersky.css` e `static/js/kaspersky.js`. Testes `KasperskyTests` (12 casos) e fluxo conferido no navegador com o export real (25 dispositivos, 21 licencas em uso).
- Futura Digital: **corrigida a regra de cobranca**, que fechava abaixo da nota fiscal. A formula subtraia as coloridas da base do excedente (`excedentes = total - cor - franquia`), mas na memoria de calculo da Futura as coloridas **contam no volume da franquia** e ainda pagam a taxa de cor por cima. Agora: **`excedentes = max(producao_total - franquia, 0)`** e `valor = franquia + excedentes*0,07 + coloridas*0,75`. Confere exatamente com a NF (producao 56.141, franquia 23.000, coloridas 498 -> 33.141 excedentes, R$ 2.319,87 + R$ 373,50 + R$ 1.610,00 = **R$ 4.303,37**) e com a fatura de 05/2026 (44.436/204 -> R$ 3.263,52), que era justamente o mes que nao batia com a formula antiga. A diferenca entre as duas formulas e sempre `coloridas x 0,07` (R$ 12 a R$ 35 por mes).
- Futura Digital: o campo **"Total de copias no mes" virou "Producao total do mes"**, com texto de ajuda explicando que se deve informar a producao total da memoria de calculo (todos os contadores, **ja incluindo as coloridas**) e nao descontar nada — a confusao que gerou o lancamento errado. O bloco de calculo ao vivo agora mostra "Producao P&B (informativo)", "Copias excedentes (producao - franquia)" e a conta de cada linha; na tela, o cartao e a coluna "Copias" viraram "Producao" (e a metrica do grafico tambem). Ajustes em `core/models.py` (`calcular_excedentes`), `static/js/futura_digital.js`, `templates/partials/futura_digital_modal.html` e `templates/chamados/futura_digital.html`. **As faturas ja cadastradas nao foram recalculadas** (guardam o valor efetivamente pago); ao editar uma fatura antiga, ela passa a ser recalculada pela regra nova. Testes `FuturaDigitalTests` atualizados/adicionados (valores da NF de 07/2026, fatura de 05/2026 e producao abaixo da franquia) e calculo ao vivo conferido no navegador.
- Requisicoes: novo botao **"Nao aprovar"** no rodape do detalhe (TI/admin) para registrar que a compra foi **recusada**. Ao clicar, a requisicao passa para o novo status **"Nao aprovada"** (badge vermelho na lista e no detalhe, migration `0045` — so `AlterField` das choices), a aprovacao de qualquer orcamento e removida (nada sera comprado) e o evento entra na timeline ("Requisicao marcada como NAO APROVADA por X."). O botao **alterna**: com a requisicao ja recusada ele vira **"Reabrir requisicao"** (ambar) e devolve para "Esperando aprovacao", tambem registrado na timeline. Bloqueado apos a entrega (`409`) e oculto no detalhe de requisicao entregue; `403` para usuario comum e `405` fora do POST. Nova rota `POST /contratos/requisicoes/<id>/nao-aprovar/` (name `requisicao_nao_aprovar`, view `requisicao_nao_aprovar_view`). E diferente de "Desaprovar requisicao", que apenas tira a aprovacao dos orcamentos e mantem a compra em andamento. Ajustes em `core/models.py` (`STATUS_NAO_APROVADA`), `core/views.py`, `core/urls.py`, `templates/chamados/contratos.html`, `templates/partials/requisicao_detail_modal.html`, `static/js/contratos.js` (`naoAprovarRequisicao`/`syncNaoAprovarButton`) e `static/css/contratos.css` (badge `--nao_aprovada`). Testes: 5 casos em `OrcamentoAprovacaoTests` (marca, limpa aprovacoes, alterna/reabre, bloqueio apos entregue, metodo e permissao) + conferido no navegador com Playwright.
- Requisicoes: as duas copias (**WhatsApp** e **e-mail**) deixaram de levar a linha "Tipo: ... | Status: ..." — tipo e status sao controle interno e nao interessam a quem recebe a mensagem. O cabecalho agora vai direto do codigo/titulo para "Solicitado por ... em ...".
- Requisicoes: ao lado de "Copiar p/ WhatsApp", o detalhe da requisicao ganhou o botao **"Copiar p/ e-mail"** (azul), que copia a requisicao inteira **formatada em HTML, com as fotos dos produtos embutidas**, pronta para colar no corpo do e-mail. O conteudo sai com cabecalho (codigo + titulo, quem solicitou e quando, entrega quando houver), descricao, e um bloco por orcamento com **foto, loja, valor unitario, quantidade, frete, desconto, link do produto (clicavel), total, documentos anexados (nomes)** e os **suborcamentos indentados**, fechando com o total do orcamento + suborcamentos e um rodape discreto. As fotos vao como **data URI** (baixadas na hora pela rota protegida e convertidas em base64), para a imagem aparecer para quem recebe o e-mail mesmo estando fora do sistema; foto acima de 3 MB nao entra no corpo (fica o aviso "foto do produto disponivel no sistema"). A copia leva as duas versoes: **text/html** (formatada) e **text/plain** (mesmo conteudo em texto puro, para editores sem formatacao). Usa `navigator.clipboard.write` com `ClipboardItem` em HTTPS e, em HTTP/navegador antigo, cai para a copia de uma selecao oculta (`execCommand`), que tambem preserva a formatacao. Recurso **so no frontend** (sem rota/model/migration), a partir do `currentDetailData` que o detalhe ja carrega. Novas funcoes em `static/js/contratos.js` (`fetchFotoDataUrl`, `coletarFotos`, `buildEmailHtml`, `buildEmailPlainText`, `copyRich`, `copyRequisicaoEmail` + `EMAIL_STYLES` com os estilos inline, ja que cliente de e-mail ignora `<style>`), botao em `templates/partials/requisicao_detail_modal.html` e ajuste do rodape em `static/css/contratos.css` (os dois botoes de copiar agora ficam juntos a esquerda). Validado no navegador (Playwright): HTML na area de transferencia com as fotos embutidas, valores e documentos, sem erro de JS.
- Kanban: chamado parado em **"aguardando" (usuario/peca/autorizacao) agora pode ser encerrado sem dar Play**. O card nessa situacao passa a mostrar **Play e Stop** (o Pause continua oculto, pois nao ha atendimento rodando); o Stop abre o mesmo modal de encerramento, com um aviso de que o chamado sera fechado sem atendimento ativo e com o "O que foi feito" **obrigatorio** como sempre. Antes era preciso dar Play so para poder fechar, criando um periodo de atendimento artificial. **Linha do tempo:** o encerramento direto nao cria periodo em `AtendimentoHistorico`, mas registra na historia do chamado a **mudanca de status** (de qual "aguardando" ele saiu) e um **evento de encerramento** com quem fechou, de onde saiu e o texto do que foi feito — novo tipo `encerramento_direto` em `ChamadoEvento` (migration `0044`, so `AlterField` das choices), que aparece tanto no "Historico do chamado" quanto no "Andamento do atendimento" do detalhe, em ordem cronologica junto dos atendimentos anteriores. A notificacao de fechamento por e-mail continua sendo disparada. Backend em `core/views.py`: `finish_attendance_view` aceita Stop sem atendimento ativo apenas para chamado em "aguardando" (fora disso responde `409` "Inicie o atendimento (Play) antes de finalizar este chamado."; Pause sem Play continua `409`), `_close_chamado_on_stop` ganhou o parametro `sem_atendimento_ativo` e `_serialize_ticket_timeline` passou a juntar os encerramentos diretos ao andamento. Front: flag `can_close_direct` no card (`_serialize_kanban_card`), botao Stop liberado em `templates/partials/kanban_card.html`, aviso no `partials/atendimento_modal.html` (estilo `.attendance-modal__notice`) e `syncDirectCloseButton`/`isDirectClose` em `static/js/chamados.js` mantendo o botao coerente sem refresh (ex.: apos um Pause com motivo o Stop continua disponivel). Testes: 8 casos novos em `EncerramentoChamadoTests` (fecha em aguardando, linha do tempo completa, bloqueio fora de aguardando, Pause sem Play, descricao obrigatoria, chamado ja fechado, permissao e a flag do card).
- Kanban: corrigido o contador **"em atend."** da coluna do atendente, que podia mostrar **2 (ou mais) com apenas 1 chamado realmente com Play**. A quebra por status era calculada a partir do `status` gravado no chamado, e existiam chamados com `em_atendimento` no banco **sem nenhum atendimento ativo** — resquicio de antes de 16/07/2026, quando arrastar o card para a coluna do atendente ja marcava "Em atendimento" (hoje marca "Atribuido" e o "Em atendimento" so vale do Play ao Pause/Stop). Duas correcoes: (1) novo helper `_status_efetivo` em `core/views.py`, usado pelo `_serialize_kanban_card`, que mostra o chamado pelo que ele realmente e — `em_atendimento` **sem Play ativo** vira "Atribuido" (com atendente) ou "Aberto" (sem atendente), corrigindo badge, esmaecido e contadores de uma vez e impedindo que a divergencia volte a aparecer; e (2) migration de dados `0043` que normaliza os registros antigos no banco (chamados nao encerrados com `em_atendimento` e sem atendimento aberto voltam para "Atribuido"/"Aberto"), sem alterar `atualizado_em` (a ordenacao das colunas continua igual). Sem mudanca de rota, model ou frontend. Testes `test_status_em_atendimento_sem_play_conta_como_atribuido` e `test_status_em_atendimento_sem_play_e_sem_atendente_conta_como_aberto` em `EncerramentoChamadoTests` (e `test_dashboard_stats_por_atendente` ajustado para iniciar o Play).

## 2026-07-24

- Requisicoes: no modal de detalhe da requisicao ganhou o botao **"Copiar p/ WhatsApp"** (rodape, verde a esquerda), que copia para a area de transferencia a requisicao inteira (codigo, titulo, tipo, status, solicitante/data, texto e todos os orcamentos com seus suborcamentos: loja, valor, quantidade, frete/desconto quando != 0 e totais) ja **formatada com a marcacao do WhatsApp** (negrito com `*asterisco*`, italico com `_underline_`, separadores e emojis) e **sem links** (a pedido, para a mensagem ficar limpa). Recurso **so no frontend** (sem rota/model/migration): usa os dados que o `requisicao_detail_view` ja devolve, agora guardados em `currentDetailData` no `static/js/contratos.js`. Novas funcoes `buildWhatsappMessage` (monta o texto), `fmtRawMoney` (formata os valores crus no padrao BR igual ao `_fmt_money` do backend), `copyText` (usa `navigator.clipboard` com fallback `execCommand` para HTTP/navegador antigo) e `copyRequisicaoWhatsapp` (copia + toast). Botao em `templates/partials/requisicao_detail_modal.html` e estilo `.requisicao-detail__whatsapp` em `static/css/contratos.css` (rodape agora alinhado a direita com o botao do WhatsApp empurrado a esquerda por `margin-right:auto`).
- Requisicoes: ao **criar um suborcamento**, o formulario ganhou a opcao **"Criar este suborcamento em todos os orcamentos da requisicao"** (checkbox desmarcado por padrao, visivel so na criacao de suborcamento — nao em orcamento nem na edicao). Marcado, o mesmo suborcamento (campos + foto + documentos) e replicado em **cada orcamento principal** da requisicao (inclusive o atual); desmarcado, mantem o comportamento antigo (so no orcamento atual). Backend: `suborcamento_create_view` le `aplicar_todos_orcamentos` e itera os orcamentos, com o helper `_criar_suborcamento` (reaproveita os arquivos de upload reposicionando o ponteiro com `seek(0)`, tambem aplicado em `_salvar_documentos`); a resposta traz `criados` (quantidade). Front: checkbox em `templates/partials/orcamento_form_modal.html`, exibicao condicional em `static/js/contratos.js` (`openOrcamentoForm`) e estilo `.sub-apply-all` em `static/css/contratos.css`. Testes em `RequisicaoEdicaoTests` (so no atual, replicado em todos, permissao).

- Kanban: chamados em "aguardando" (usuario/peca/autorizacao) agora aparecem **esmaecidos** (semitransparentes) na coluna do atendente, sinalizando que estao parados esperando algo externo; hover/foco devolve a opacidade cheia. Alem disso, cada coluna de atendente ganhou uma **quebra de contadores** abaixo do total: **Em atendimento** (laranja), **Aguardando** (cinza, soma dos 3 tipos) e **Atribuido** (roxo). Os numeros vem do backend na carga (`_column_stats` em `core/views.py`, campo `stats` por atendente) e sao recalculados no front sem refresh a cada Play/Pause/Stop, movimentacao e conversao de pendencia (`updateColumnBreakdown` em `static/js/chamados.js`, a partir do novo `data-ticket-status` do card). O card serializado ganhou `is_waiting`; o card template ganhou a classe `ticket-card--waiting` e `data-ticket-status`. Estilos em `static/css/chamados.css` (`.ticket-card--waiting`, `.column-breakdown`, `.column-stat`). Teste `test_dashboard_stats_por_atendente` em `EncerramentoChamadoTests`.
- Kanban: os contadores da quebra por status agora sao **clicaveis** e fazem um **preview temporario** — clicar em "em atend." (ou "aguard." / "atrib.") esconde os demais cards da coluna e mostra so os daquele status por ~20s, voltando sozinho ao padrao (ou ao clicar de novo/em outro, ou ao arrastar). Contadores zerados nao sao clicaveis. E apenas um recorte visual no cliente (nao altera dados). Implementado em `static/js/chamados.js` (`applyStatFilter`/`clearStatFilter`/`initializeColumnStatFilters`, classe `ticket-card--filtered-out`), com os contadores recebendo `role="button"`/`tabindex` no template e estilos de hover/ativo em `static/css/chamados.css` (`.column-stat--active`, `.ticket-card--filtered-out`).
- Kanban: **bloqueado mover um chamado enquanto ele estiver com atendimento ativo (Play)**. Antes, arrastar um chamado com Play ativo para outro atendente trocava o `atendente_atual` mas deixava o periodo de atendimento (cronometro) ainda aberto e vinculado ao atendente original — o novo atendente recebia o card "Em atendimento" sem conseguir pausar/finalizar (o Play nao era dele). Agora o drop e recusado para qualquer destino (coluna de atendente ou "Chamados abertos"): o card volta para a origem e aparece "Pause ou finalize o atendimento (Play) antes de mover o chamado.". Validado no frontend (`static/js/chamados.js`, via `data-ticket-active`) e no backend (`move_ticket_view` recusa com `409` quando ha `AtendimentoHistorico` ativo). Basta Pause/Stop para liberar. Removido o ramo (agora inalcancavel) que preservava "Em atendimento" ao mover com Play ativo. Testes `test_mover_chamado_com_play_ativo_e_bloqueado`, `test_mover_para_abertos_com_play_ativo_e_bloqueado` e `test_move_liberado_apos_pausar_o_atendimento` em `EncerramentoChamadoTests`.
- E-mail (notificacoes): quando o **solicitante do chamado e da propria TI** (Atendente TI/Admin), ele deixa de receber a **copia pessoal** ("Seu chamado foi aberto com sucesso" e as copias de solicitante nos demais eventos) e passa a ser notificado **apenas pela lista de e-mails da TI**. Corrige o caso do atendente que abre um chamado para si mesmo no Kanban ou converte uma pendencia que ele proprio criou e, por estar tambem na lista `ti@...` (que chega na caixa dele), recebia **dois e-mails** da mesma acao: um pessoal e outro pela TI. O dedup por endereco (que ja evitava duplicidade quando o e-mail pessoal esta literalmente na lista da TI) continua valendo. Ajuste apenas em `core/emails.py`: novos helpers `_solicitante_eh_ti` e `_email_solicitante_notificar` (retorna vazio quando o solicitante e TI), usados por `notificar_novo_chamado`, `notificar_nova_mensagem`, `notificar_mudanca_status` e `notificar_fechamento`. Sem alteracao de model, rota ou migration. Teste `test_solicitante_da_ti_nao_recebe_copia_pessoal` em `EmailNotificacaoTests`.

## 2026-07-23

- Requisicoes: adicionada a acao **"Desaprovar requisicao"** (botao no rodape do detalhe, TI/admin), que aparece quando a requisicao tem um orcamento aprovado (status "Aguardando entrega"). Ao clicar, a aprovacao de **todos os orcamentos** e removida e a requisicao volta para "Esperando aprovacao", com evento na timeline. Nova rota `POST /contratos/requisicoes/<id>/desaprovar/` (name `requisicao_desaprovar`); exige ao menos um orcamento aprovado (`409` se nao houver) e e bloqueada apos a entrega (`409`). Ajustes em `core/views.py` (`requisicao_desaprovar_view`), `core/urls.py`, `templates/partials/requisicao_detail_modal.html`, `templates/chamados/contratos.html` e `static/js/contratos.js`. Testes adicionados a `OrcamentoAprovacaoTests` (desaprova limpa todos e volta o status, `409` sem aprovado, bloqueio apos entregue, permissao e metodo).
- Requisicoes: correcao dos **codigos** das requisicoes por ordem de criacao (migration de dados `0042`). A requisicao mais recente passa a ser `REQ-00051` e as anteriores recebem os numeros decrescendo por data (`REQ-00050`, `REQ-00049`, ...). Roda uma vez por banco no proximo deploy/migrate; em banco sem requisicoes (dev/clone) e no-op. Como `gerar_codigo` parte sempre do maior codigo existente, a proxima requisicao cadastrada continua em `REQ-00052`. Testes `RequisicaoRenumeracaoTests`.
- Requisicoes: agora e possivel **editar** uma requisicao existente e os seus **orcamentos/suborcamentos**. No modal de detalhe, o botao "Editar requisicao" (rodape, TI/admin) reabre o formulario pre-preenchido para alterar titulo, tipo e texto (`POST /contratos/requisicoes/<id>/editar/`, name `requisicao_edit`), registrando um evento de edicao na timeline (novo tipo `TIPO_EDICAO` em `RequisicaoContratoEvento`, migration `0041`). Cada orcamento e cada suborcamento ganhou um botao "Editar" que abre o mesmo formulario de cadastro em modo edicao: altera os campos (titulo, loja, moeda, valor, quantidade, frete, desconto, link), permite **trocar ou remover a foto do produto** e **adicionar ou remover documentos** ja anexados (checkboxes com a lista atual). Rotas `POST /contratos/orcamentos/<id>/editar/` (name `orcamento_edit`) e `POST /contratos/suborcamentos/<id>/editar/` (name `suborcamento_edit`); a edicao de orcamento/suborcamento e **bloqueada com `409`** quando a requisicao ja foi entregue (mesma regra da aprovacao) e nao altera o estado de aprovacao. Os arquivos removidos (foto substituida/removida e documentos apagados) saem do disco pelos signals `post_delete` de `core/signals.py`. A serializacao de documentos passou a expor o `id` (necessario para remove-los). Ajustes em `core/views.py` (views `requisicao_edit_view`, `orcamento_edit_view`, `suborcamento_edit_view` + helper `_aplicar_edicao_item`), `core/urls.py`, `templates/partials/requisicao_create_modal.html`, `requisicao_detail_modal.html`, `orcamento_form_modal.html`, `static/js/contratos.js` e `static/css/contratos.css`. Testes `RequisicaoEdicaoTests` (edicao de requisicao com evento, titulo curto, permissao, metodo; edicao de orcamento/suborcamento, bloqueio apos entregue, remocao de documento com limpeza do disco).

## 2026-07-20

- Emprestimos: agora e possivel **editar** um emprestimo existente (botao "Editar emprestimo" no detalhe, apenas TI/admin) para **adicionar** equipamentos, **marcar como devolvido** (com data) ou **remover** equipamentos — mantendo um contrato unico por pessoa. Cada equipamento passou a ter `data_emprestimo` e `data_devolucao` proprias (model `EquipamentoEmprestimoTI`, migration `0040` com backfill da data a partir do emprestimo), permitindo uma linha do tempo quando os itens sao emprestados/devolvidos em momentos diferentes. Qualquer edicao **regera o termo em PDF** e **descarta o termo assinado anterior**; se ainda houver item em posse do colaborador o status volta para "Aguardando documentacao assinada" (para nova assinatura), e se todos foram devolvidos o status passa para "Devolvido". No termo, os equipamentos ativos aparecem com "Emprestado em <data>" e ha uma nova secao "Equipamentos ja devolvidos" (data de emprestimo + devolucao). Nova rota `POST /emprestimos/<id>/editar/` (name `emprestimo_editar`); o detalhe passou a expor as datas por equipamento e um bloco `edit` com os valores crus para o formulario. Adicionado signal `post_delete` para `FotoEquipamentoEmprestimoTI` (apaga a foto do disco ao remover o equipamento). Novos arquivos: `partials/emprestimo_edit_modal.html`; ajustes em `static/js/emprestimos.js`, `static/css/emprestimos.css`, `core/termo_pdf.py`. Testes `EmprestimoEdicaoTests` (adicionar, devolver, nao remover todos, permissao).
- Deploy: o `scripts/deploy.sh` passou a **autodetectar** a env de producao — usa `CHAMADOS_ENV` se definido, senao o `.env` da raiz do projeto (que o `settings.py` ja carrega) e, por fim, `/etc/chamados/app.env`. Elimina o aviso indevido de env nao encontrada em servidores que guardam as variaveis no `.env` do projeto (caso deste deploy, em `/opt/chamados/.env`).
- Deploy: adicionado o script `scripts/deploy.sh` e o atalho global `chamados-deploy` para publicar qualquer alteracao com um comando so. O script faz o ciclo completo: carrega o `/etc/chamados/app.env`, `git pull` (`--ff-only`), `pip install -r requirements.txt`, `migrate`, `collectstatic`, `manage.py check --deploy` e `systemctl restart` do servico, so declarando sucesso se o servico subir (senao aponta o `journalctl`). Ambiente configuravel por variaveis (`CHAMADOS_DIR`, `CHAMADOS_VENV`, `CHAMADOS_ENV`, `CHAMADOS_SERVICE`, `CHAMADOS_BRANCH`), com defaults iguais aos do `docs/08_deploy_seguro.md`. Para nao corromper a execucao quando o proprio `deploy.sh` e atualizado pelo `git pull`, o script se re-executa a partir de uma copia temporaria. Instalacao do atalho (symlink) e liberacao do `systemctl` no sudoers documentadas na nova secao 5c do `docs/08_deploy_seguro.md`. Adicionado `.gitattributes` forcando LF nos `*.sh`.
- Emprestimos: corrigido o erro "Anexe o termo assinado antes de marcar como OK" que aparecia mesmo apos o usuario escolher um arquivo. A causa era o fluxo em dois passos do modal de detalhe: escolher o arquivo no campo nao envia nada; era preciso clicar antes em "Anexar termo assinado" para so entao "Marcar documentacao OK". Quem selecionava o arquivo e clicava direto em "Marcar documentacao OK" caia no `409` do backend (o `termo_assinado` ainda estava vazio). Agora o botao "Marcar documentacao OK" **anexa automaticamente** o arquivo que estiver selecionado no campo (mas ainda nao enviado) antes de marcar, eliminando a confusao. Ajuste apenas no frontend (`static/js/emprestimos.js`): funcao `anexarTermo()` reutilizada pelos dois caminhos e o botao "Marcar OK" fica desabilitado durante a requisicao. Sem alteracao de model, rota ou backend.

## 2026-07-17

- Correcao de travamento ao alternar rapido entre os menus: o stream SSE de notificacoes (`/notificacoes/stream/`) segurava uma **thread do gunicorn (gthread)** por conexao e so detectava a saida do navegador a cada **~16s**. Ao trocar de menu, cada pagina nova abria um novo `EventSource` e a conexao antiga ficava pendurada, acumulando ate esgotar as threads (3 workers x 16 = 48) — as paginas seguintes ficavam "carregando" ate uma conexao velha expirar. Ajustes no `notificacoes_stream_view`: **heartbeat a cada ~2s** (detecta a desconexao e libera a thread em ~2s, nao 16s) e **reciclagem da conexao a cada ~55s** (o `EventSource` reconecta sozinho), garantindo que a thread sempre seja liberada. Sem mudanca de comportamento visivel (comentarios SSE sao ignorados pelo cliente).
- Pendencias: cada pendencia ganhou uma **prioridade por cor** em 5 niveis (vermelho = mais urgente ... verde = menos urgente). A cor e escolhida por **swatches** tanto no modal de **cadastro** quanto no de **detalhe** (ao clicar no card), com atualizacao imediata (sem refresh). Por padrao a pendencia entra **sem cor (card branco)**; so fica **pintada** quando um nivel e escolhido (fundo em tom suave via `color-mix`, com faixa e borda mais fortes; fallback branco em navegadores sem `color-mix`), mantendo o titulo legivel. A coluna "Pendencias" e **ordenada por prioridade** (vermelho no topo), depois as **sem cor por ultimo**, e dentro do mesmo nivel pelos mais recentes; ao criar/alterar a prioridade o card e reposicionado sozinho. Campo `prioridade` (inteiro 1..5, **anulavel**, default `None` = sem cor) no model `PendenciaTI`, com a tabela de cores/rotulos em `PendenciaTI.PRIORIDADES` (fonte unica). Migrations `0037` (campo), `0038`/`0039` (ordering com nulos por ultimo + campo anulavel; `0039` tambem zera a cor das pendencias ja existentes). Nova rota `POST /chamados/pendencias/<id>/prioridade/` (name `pendencia_priority`, TI/admin). (Substitui a ordenacao anterior, que era so por data de criacao.)

## 2026-07-16

- Requisicoes: fluxo de aprovacao/entrega alinhado ao sistema antigo. Aprovar um orcamento agora move a requisicao para **"Aguardando entrega"** (antes ia direto para "Finalizada") e o botao do orcamento aprovado vira **"Marcar entregue"**; ao marcar, a requisicao vai para **"Entregue"** (com data e responsavel). Novos status `aguardando_entrega` e `entregue` no `RequisicaoContrato`. Nova rota `POST /contratos/requisicoes/<id>/marcar-entregue/` (name `requisicao_marcar_entregue`, TI/admin). Nao e possivel alterar a aprovacao depois de entregue.
- Requisicoes: cada requisicao ganhou um **codigo** sequencial (`REQ-00049`, `REQ-00050`, ...), continuando a numeracao do sistema antigo, que parou em `REQ-00048` (48 requisicoes no banco antigo). O codigo e gerado no cadastro (campo `codigo` unico) e aparece na lista e no cabecalho do detalhe. A migration `0036` faz o backfill das requisicoes ja existentes na ordem de criacao, comecando em `REQ-00049`.
- Requisicoes: adicionado o **historico/timeline** da requisicao (model `RequisicaoContratoEvento`), registrando criacao, aprovacao/desaprovacao de orcamento e entrega, cada um com autor e data/hora. O detalhe da requisicao mostra a timeline (mais recentes no topo), permitindo saber o dia em que foi aprovada e o dia em que foi entregue. Migration `0036` (campos `codigo`, `entregue_em`, `entregue_por` + model de eventos). Testes: aprovacao->aguardando_entrega, `marcar_entregue`, geracao de codigo. (Nota: substitui o comportamento anterior do mesmo dia, em que aprovar movia a requisicao direto para "Finalizada".)

- Kanban: arrastar um chamado para a coluna de um atendente agora apenas **atribui** o chamado (novo status "Atribuido"), em vez de marca-lo direto como "Em atendimento". O status "Em atendimento" passou a valer **somente enquanto ha um atendimento ativo** (do Play ate o Pause/Stop). Fluxo: arrastar -> Atribuido; Play -> Em atendimento; Pause sem motivo -> volta para Atribuido; Pause com motivo -> Aguardando (usuario/peca/autorizacao); Stop -> Fechado. Se o chamado ja tiver um Play ativo, o "Em atendimento" e preservado ao mover. A conversao de pendencia em chamado tambem entra como "Atribuido". Novo status `atribuido` (badge roxo `status-assigned`); nao houve migration para o `Chamado` (o campo `status` nao usa `choices` no banco).
- Kanban: ao arrastar um chamado para um atendente, o card passa a aparecer no **topo da coluna, logo abaixo dos que estao com Play ativo** (antes ficava onde era solto). No recarregamento, cada coluna de atendente ordena os cards com Play ativo no topo e, dentro de cada grupo, os de atividade mais recente primeiro (por `atualizado_em`). Chamado recem-convertido de pendencia tambem entra no topo abaixo dos ativos.
- Kanban: a coluna "Pendencias" passou a listar as pendencias **mais recentes no topo** (ordenacao por data/hora de criacao decrescente), tanto no recarregamento quanto ao criar uma nova pendencia (inserida no topo, nao no fim).
- Requisicoes: adicionada a **aprovacao de um orcamento** especifico dentro de uma requisicao. Cada orcamento no modal de detalhe ganhou o botao "Aprovar orcamento" / "Remover aprovacao". A aprovacao e **exclusiva** (apenas um orcamento aprovado por requisicao; aprovar um remove a aprovacao dos demais) e move a requisicao para "Finalizada"; remover a aprovacao (quando nenhum orcamento fica aprovado) volta a requisicao para "Esperando aprovacao". O orcamento aprovado fica destacado (borda verde + chip "Aprovado em ...") e o badge de status da requisicao atualiza na lista e no detalhe sem refresh. Adicionados os campos `aprovado`, `aprovado_em` e `aprovado_por` ao model `OrcamentoContrato` (migration `0035`), a rota `POST /contratos/orcamentos/<id>/aprovar/` (name `orcamento_aprovar`, TI/admin, alterna o estado) e testes `OrcamentoAprovacaoTests`. Teste `test_move_para_atendente_marca_atribuido_nao_em_atendimento` cobre o novo fluxo do Kanban.

## 2026-07-15

- Adicionado o modulo "E-mail" (menu lateral, apenas TI/admin): notificacoes automaticas de chamados por e-mail. Toda vez que um chamado e aberto, o sistema envia e-mail para **quem abriu** (confirmacao) e para o(s) **e-mail(s) da TI**; e ha notificacao tambem nas **alteracoes**: nova mensagem na conversa (avisa a outra parte, sem copiar quem escreveu), mudanca de status no Kanban e fechamento pelo Stop (com o "o que foi feito"). Cada evento tem liga/desliga proprio e o envio so ocorre com a chave "Ativar notificacoes" ligada. Os disparos foram integrados nas views de abrir chamado (portal), criar no Kanban, converter pendencia, enviar mensagem, mover card e encerrar atendimento.
- Criada a tela de configuracao `/email-config/` (acesso TI/admin) com botao "E-mail" no menu lateral: formulario dos dados SMTP (servidor, porta, TLS/SSL, timeout, conta de envio, remetente, e-mails da TI e checkboxes dos eventos) ja com os **defaults do Google/Gmail** (`smtp.gmail.com`, 587, TLS), mais um formulario a parte para **enviar e-mail de teste** (mostra o erro real do SMTP) e um guia rapido de como gerar a senha de app do Google. Rotas `/email-config/salvar/` e `/email-config/testar/` (POST, TI/admin).
- Criado o model `EmailConfig` (singleton, migration `0031`), registrado no admin sem expor a senha. A **senha de app do Google e cifrada em repouso** com Fernet (`core/crypto.py`, mesma `VAULT_ENCRYPTION_KEY` do Cofre) — no banco so ha texto cifrado; salvar o campo em branco mantem a senha atual e ha opcao de remove-la (espacos sao removidos, pois o Google mostra a senha em blocos de 4). TLS e SSL nao podem ser ligados juntos. Todo o envio real fica em `core/emails.py`, **fail-safe**: qualquer falha de SMTP e apenas logada e nunca interrompe o fluxo do chamado. Adicionada a setting de suporte `EMAIL_BACKEND_OVERRIDE` (usada nos testes para capturar os e-mails em memoria). Novos arquivos: `core/emails.py`, `templates/chamados/email_config.html`, `static/css/email_config.css`. Testes automatizados (`core/tests.py`: `EmailNotificacaoTests`, 13 casos) cobrindo o disparo de cada evento, os destinatarios corretos, notificacoes desligadas, a cifra da senha, a manutencao da senha na edicao, as permissoes, o e-mail de teste e a tolerancia a falha (chamado abre mesmo sem e-mail configurado).

## 2026-07-14

- Adicionado o modulo "Cofre" (menu lateral, apenas TI/admin) — cofre de senhas da empresa, migrado do app `cofre` do sistema antigo com as 20 credenciais. Seguranca em duas camadas: (1) **cifra em repouso** com Fernet (`core/crypto.py`) usando a chave `VAULT_ENCRYPTION_KEY` (variavel de ambiente, NUNCA no codigo/Git; em dev deriva da SECRET_KEY) — no banco so ha texto cifrado; e (2) **senha-mestra** (guardada como hash) que destrava o cofre na sessao por `VAULT_UNLOCK_SECONDS` (padrao 15 min) com **auto-lock** e **bloqueio anti-brute-force** (`VAULT_MAX_FAILED_ATTEMPTS`/`VAULT_LOCKOUT_SECONDS`). A tela tem 4 estados: setup (admin define a senha-mestra no 1o acesso), travado (pede a senha-mestra), bloqueado (lockout) e aberto (grade de credenciais). As senhas aparecem mascaradas e sao **reveladas/copiadas uma a uma sob demanda** (endpoint JSON `revelar`, nunca todas no HTML); ha busca, timer de auto-lock, botao de travar e CRUD por modal. Toda acao (destrave, falha, bloqueio, lock, senha-mestra, criar/editar/excluir e revelar) e registrada em `CofreAuditoria` (com IP), visivel para admin na propria tela.
- Acesso = Atendente TI/Admin **+** senha-mestra; so **admin** define/altera a senha-mestra (para trocar, exige a atual). Criados os models `CofreConfig` (singleton: hash da senha-mestra, tentativas, lockout), `CofreCredencial` (rotulo, usuario, senha cifrada, notas) e `CofreAuditoria` (migration `0029`), registrados no admin sem expor a senha. O seed inicial vem da migration `0030`, que le `seed/cofre_seed.json` (senhas em TEXTO, arquivo LOCAL ignorado pelo Git) e as grava ja cifradas — **apagar o arquivo apos semear**. Adicionadas as settings `VAULT_ENCRYPTION_KEY`, `VAULT_ALLOW_INSECURE_KEY_DERIVATION`, `VAULT_UNLOCK_SECONDS`, `VAULT_MAX_FAILED_ATTEMPTS`, `VAULT_LOCKOUT_SECONDS` e knobs de HTTPS para producao (`SECURE_COOKIES`, HSTS, SSL redirect). Adicionado `cryptography` ao `requirements.txt`. Novos arquivos: `core/crypto.py`, `static/css/cofre.css`, `static/js/cofre.js`, `templates/chamados/cofre.html` e `partials/cofre_credencial_modal.html`; item "Cofre" no menu lateral. Testes `CofreTests` e `CofreCryptoTests`. Guia de deploy seguro no Ubuntu criado em `docs/08_deploy_seguro.md`.
- Removido o campo de senha do modulo Starlinks (migration `0028`): as antenas/contas nao guardam mais credencial de acesso, conforme solicitado. Retirado do model, do formulario de cadastro/edicao, do card (nao ha mais senha mascarada nem botoes de mostrar/copiar) e do JS/CSS; o seed local `starlinks_seed.json` foi regenerado sem o campo (nao restam senhas em texto). Testes de senha ajustados.
- Adicionado o modulo "Starlinks" (menu lateral, apenas TI/admin) para as antenas/contas Starlink da empresa, migrando as 6 unidades do banco do servidor antigo. A tela mostra as Starlinks em uma grade de cards responsiva: cada card tem nome, local, badge Ativa/Inativa e os campos organizados (e-mail com link, senha, forma de pagamento + final do cartao, identificador Starlink, numero de serie, numero do kit e versao do software). Cartoes de resumo (total, ativas, inativas), pesquisa (nome, local, e-mail, serial, kit, identificador) e chips de filtro por status. A senha vem mascarada (••••), com botoes para mostrar/ocultar e copiar para a area de transferencia. O botao "Nova Starlink" e o "Editar" abrem o modal de cadastro/edicao (com todos os campos e um olho para revelar a senha ao digitar). Exclusao com confirmacao. Toast classico (Django messages).
- No sistema antigo as senhas das Starlinks eram guardadas cifradas (Fernet, via o modulo cofre). Na migracao elas foram decifradas usando a `VAULT_ENCRYPTION_KEY` do `.env` antigo e re-armazenadas em texto no campo `senha` (5 das 6 tinham senha; a unidade "Backup" estava sem). O arquivo de seed com as credenciais NAO e versionado.
- Criado o model `Starlink` (nome, local, e-mail, senha, ativo, forma de pagamento, final do cartao, identificador, versao do software, numero de serie, numero do kit) na migration `0026`, registrado no admin. Na edicao, deixar a senha em branco mantem a atual. O seed inicial vem da migration `0027`, que le um arquivo LOCAL ignorado pelo Git (`seed/starlinks_seed.json`). Idempotente. Rotas criadas (todas restritas a TI/admin no backend): `GET /starlinks/`, `POST /starlinks/criar/`, `POST /starlinks/<id>/editar/`, `POST /starlinks/<id>/excluir/`. Novos arquivos `static/css/starlinks.css`, `static/js/starlinks.js`, `templates/chamados/starlinks.html` e `partials/starlink_modal.html`; adicionado o item "Starlinks" no menu lateral. Testes automatizados (`core/tests.py`: `StarlinkTests`) cobrindo CRUD, manutencao da senha na edicao, status ativo/inativo e permissoes.
- Adicionado o modulo "Dicas" (menu lateral, apenas TI/admin) como base de conhecimento da equipe de TI, migrando as 14 dicas do banco do servidor antigo (resolucoes, configuracoes e procedimentos gerais). A tela mostra as dicas em uma grade de cards responsiva com badge colorido de categoria, titulo, previa do conteudo (3 linhas) e indicador de anexo. Chips de categoria (Todas, Geral, Configuracao, Resolucao) com contagem filtram a grade e combinam com a busca (por titulo, conteudo ou categoria). Clicar em um card abre um modal de detalhe que mostra o conteudo completo com as quebras de linha preservadas e as URLs transformadas em links clicaveis (o conteudo e escapado antes, por seguranca), alem do anexo (abre inline). O botao "Nova dica" e o lapis abrem o modal de cadastro/edicao (categoria, titulo, conteudo e anexo); na edicao e possivel substituir o anexo ou marcar para remover o atual. Exclusao com confirmacao. Toast classico (Django messages).
- Criado o model `Dica` (categoria, titulo, conteudo, anexo) na migration `0024`, registrado no admin. O seed inicial vem da migration `0025`, que le um arquivo LOCAL ignorado pelo Git (`seed/dicas_seed.json`) com as 14 dicas do banco antigo; os anexos (prints) ficam em `media/dicas/` (tambem fora do Git). Anexos sem arquivo fisico sao ignorados no seed; idempotente. Rotas criadas (todas restritas a TI/admin no backend): `GET /dicas/`, `POST /dicas/criar/`, `POST /dicas/<id>/editar/`, `POST /dicas/<id>/excluir/` e `GET /dicas/<id>/anexo/` (abertura protegida do anexo). Novos arquivos `static/css/dicas.css`, `static/js/dicas.js`, `templates/chamados/dicas.html` e os modais `partials/dica_modal.html` e `partials/dica_detail_modal.html`; adicionado o item "Dicas" no menu lateral da TI. Testes automatizados (`core/tests.py`: `DicaTests`) cobrindo CRUD, categorias, anexo (upload/remocao/download protegido) e permissoes.
- Adicionado o modulo "Futura Digital" (menu lateral, apenas TI/admin) para as faturas mensais da locacao de impressoras, migrando os cadastros e documentos do banco do servidor antigo, com toda a regra de cobranca por quantidade de impressoes. A tela tem cartoes de resumo (meses registrados, total pago, media por mes, total de copias), um GRAFICO de barras mes a mes com alternancia entre "Valor pago", "Copias" e "Excedentes" (renderizado no navegador a partir de uma serie JSON via `json_script`, sem biblioteca externa) e uma tabela responsiva (cards no celular) com mes/NF, copias, cor, excedentes, valor pago e link do documento. Busca (mes/nota) e ordenacao por cabecalho (valores por numero, mes por data). O botao "Nova fatura" abre um modal com CALCULO AO VIVO: ao digitar total de copias, coloridas, franquia e taxas, o formulario mostra os excedentes e o valor a pagar; o backend recalcula e grava (nao confia no cliente). Toast classico (Django messages).
- Regra de cobranca implementada: `copias_excedentes = max(copias_total - copias_cor - franquia_copias, 0)` (excedente conta so as copias P&B alem da franquia) e `valor_pago = franquia_valor + copias_excedentes * valor_copia_excedente + copias_cor * valor_copia_cor`. As taxas (excedente e cor), a franquia de copias e o valor da franquia sao editaveis por fatura, com defaults 0,07 / 0,75 / 23000 / 1.610,00. Confirmada contra os dados historicos (8 de 9 meses batem exatamente; maio/2026 tinha uma pequena anomalia de digitacao no sistema antigo).
- Criado o model `FuturaDigital` (mes de referencia, nota fiscal, copias total/cor, franquia de copias/valor, taxas de excedente/cor, excedentes e valor pago calculados, documento) na migration `0022`, registrado no admin. O `mes_referencia` e sempre normalizado para o 1o dia do mes (o formulario usa input `month` e aceita `AAAA-MM`). O seed inicial vem da migration `0023`, que le um arquivo LOCAL ignorado pelo Git (`seed/futura_digital_seed.json`) com os 9 meses do banco antigo, preservando os valores historicos exatos (sem recalcular); os documentos (NFs/relatorios) ficam em `media/futura_digital/` (tambem fora do Git). Idempotente.
- Rotas criadas (todas restritas a TI/admin no backend): `GET /futura-digital/`, `POST /futura-digital/criar/`, `POST /futura-digital/<id>/editar/`, `POST /futura-digital/<id>/excluir/` e `GET /futura-digital/<id>/documento/` (download protegido). Novos arquivos `static/css/futura_digital.css`, `static/js/futura_digital.js`, `templates/chamados/futura_digital.html` e `partials/futura_digital_modal.html`; adicionado o item "Futura Digital" no menu lateral da TI. Testes automatizados (`core/tests.py`: `FuturaDigitalTests`) cobrindo a regra de cobranca, normalizacao do mes, recalculo na edicao, exclusao, dados do grafico e permissoes.
- Adicionado o modulo "Contratos" (menu lateral, apenas TI/admin) para contratos/assinaturas recorrentes de TI (Google Workspace, VIVO, Kaspersky, Claude, etc.), migrando os cadastros e os arquivos do banco do servidor antigo, no mesmo esquema do modulo Servicos feitos. A tela tem cartoes de resumo (Contratos, Ativos, Total mensal dos ativos), pesquisa (client-side, por nome, forma de pagamento ou observacoes) e uma tabela responsiva (cards no celular) com nome, valor (destacado, formato brasileiro), periodicidade (Mensal/Anual/Pagamento unico), forma de pagamento (com "final XXXX"), vigencia (inicio e "ate" fim) e status (badge Ativo/Encerrado). Cabecalhos clicaveis para ordenar (Valor por numero, Vigencia por data). Clicar na linha (ou no icone de olho) abre um modal de detalhe (observacoes + anexos para download); o lapis abre a edicao. Cadastro/edicao em modal com upload de multiplos arquivos; na edicao os anexos ja cadastrados aparecem com opcao de remover. Exclusao (de contrato ou anexo) com confirmacao. Toast classico (Django messages). IMPORTANTE: e um modulo distinto de "Requisicoes" (que internamente usa o prefixo `Contrato` mas trata de requisicoes de compra) — por isso as rotas ficam em `/contratos-ti/`.
- Criados os models `Contrato` (nome, observacoes, valor opcional, forma de pagamento, final do cartao, periodicidade, inicio/fim de vigencia, encerrado_em) e `ContratoAnexo` (arquivo, nome_original) na migration `0020`, registrados no admin (com inline de anexos). Um contrato 1--N anexos (`on_delete=CASCADE`). Propriedade `esta_ativo` (True se `encerrado_em` vazio) e `valor_display` (formato brasileiro ou "-"). O seed inicial vem da migration `0021`, que le um arquivo LOCAL ignorado pelo Git (`seed/contratos_seed.json`) com os 14 contratos e 28 anexos do banco antigo; os arquivos (NFs, termos, invoices) ficam em `media/contratos_ti/` (tambem fora do Git). Anexos sem arquivo fisico sao ignorados no seed; idempotente.
- Rotas criadas (todas restritas a TI/admin no backend): `GET /contratos-ti/`, `POST /contratos-ti/criar/`, `GET /contratos-ti/<id>/` (detalhe JSON), `POST /contratos-ti/<id>/editar/`, `POST /contratos-ti/<id>/excluir/`, `GET /contratos-ti/anexos/<id>/` (download protegido) e `POST /contratos-ti/anexos/<id>/excluir/`. Novos arquivos `static/css/contratos_ti.css`, `static/js/contratos_ti.js`, `templates/chamados/contratos_ti.html` e os modais `partials/contrato_ti_modal.html` e `partials/contrato_ti_detail_modal.html`; adicionado o item "Contratos" no menu lateral da TI. Testes automatizados (`core/tests.py`: `ContratoTests`) cobrindo CRUD, anexos, valor brasileiro, periodicidade, status, cascata, download protegido e permissoes.
- Unificado o menu lateral para TI/admin: as telas do portal (Meus Chamados, Abrir Chamado e detalhe do chamado) passaram a usar o mesmo menu completo (`ti_sidebar`) das demais telas quando o usuario e Atendente TI/Admin, em vez do menu reduzido do portal. Antes, ao navegar para essas telas o menu "encolhia" para poucos itens (Meus Chamados, Abrir Chamado, Chamados, Permissoes) e reaparecia o titulo antigo "Sistema de Chamados TI / Abra novos chamados...". Agora o menu fica fixo e consistente (todos os modulos) e so troca o conteudo. O `portal_sidebar` (usado apenas pelo usuario comum) foi limpo: adotou a marca compacta (so o simbolo "TI", sem titulo/descricao) e o rodape com apenas o nome do usuario, e teve removidos os itens "Chamados" e "Permissoes" (que so apareciam para TI/admin e agora nunca usam esse menu), ficando so "Meus Chamados" e "Abrir Chamado".
- Adicionado o modulo "Servicos feitos" (menu lateral, apenas TI/admin) para o registro de servicos de TI ja executados, migrando os cadastros e os arquivos do banco do servidor antigo. A tela tem cartoes de resumo (Servicos feitos, Valor total, Anexos), um campo de pesquisa (client-side, por servico, empresa ou descricao) e uma tabela responsiva (que vira lista de cards no celular) com nome do servico, empresa, data, valor (destacado, formato brasileiro) e contagem de anexos. Os cabecalhos sao clicaveis para ordenar (Servico, Empresa, Data, Valor, Anexos) alternando crescente/decrescente com seta; a Data e o Valor ordenam corretamente (por data/numero, nao como texto). Clicar na linha (ou no icone de olho) abre um modal de detalhe com a descricao completa e os anexos para download; o icone de lapis abre o modal de edicao. O cadastro/edicao acontece em um modal com upload de multiplos arquivos (`multipart/form-data`); na edicao os anexos ja cadastrados aparecem listados com opcao de remover cada um. A exclusao (de servico ou anexo) pede confirmacao. Notificacoes pelo toast classico (Django messages).
- Criados os models `ServicoFeito` (nome, empresa, descricao, data, valor, anexos_total, valor_display) e `ServicoFeitoAnexo` (arquivo, nome_original) na migration `0018`, registrados no admin (com inline de anexos). Um servico 1--N anexos (`on_delete=CASCADE`). O valor aceita o formato brasileiro (1.234,56) e e convertido para Decimal no backend. Excluir um servico remove os anexos em cascata e apaga os arquivos do disco; ha rota para remover um anexo isolado. O seed inicial vem da migration de dados `0019`, que le um arquivo LOCAL ignorado pelo Git (`seed/servicos_feitos_seed.json`) com os 3 servicos e 8 anexos do banco antigo; os PDFs (NFs/orcamentos) ficam em `media/servicos_feitos/` (tambem fora do Git). Anexos cujo arquivo fisico nao existir sao ignorados no seed; idempotente.
- Rotas criadas (todas restritas a TI/admin no backend; usuario comum e redirecionado ou recebe `403`/`404`): `GET /servicos-feitos/`, `POST /servicos-feitos/criar/`, `GET /servicos-feitos/<id>/` (detalhe JSON), `POST /servicos-feitos/<id>/editar/`, `POST /servicos-feitos/<id>/excluir/`, `GET /servicos-feitos/anexos/<id>/` (download protegido) e `POST /servicos-feitos/anexos/<id>/excluir/`. Novos arquivos `static/css/servicos_feitos.css`, `static/js/servicos_feitos.js`, `templates/chamados/servicos_feitos.html` e os modais `partials/servico_modal.html` e `partials/servico_detail_modal.html`; adicionado o item "Servicos feitos" no menu lateral da TI. Testes automatizados (`core/tests.py`: `ServicoFeitoTests`) cobrindo CRUD, anexos, valor brasileiro, cascata, download protegido e permissoes.
- Adicionado o modulo "IPs" (menu lateral, apenas TI/admin) para o inventario de IPs/equipamentos da rede interna, migrando os dados do banco do servidor antigo. A tela tem cartoes de resumo (Total de IPs, Categorias, Com acesso salvo), um campo de pesquisa inteligente (client-side, filtra por IP, nome, fabricante, MAC, acesso ou categoria) e chips de filtro por categoria (Todas, Servidores, Switchs, IdFace + Catracas, Impressoras, Wi-Fi, Zabbix & Grafana) com contagem, que combinam com a busca. A lista e uma tabela responsiva (que vira lista de cards no celular) com badge colorido por categoria, IP e MAC em fonte mono, nome, fabricante e acesso, alem de um botao de editar por linha. O cadastro/edicao acontece em um modal Bootstrap reutilizado (create/edit) e a exclusao pede confirmacao inline no modal ("Excluir definitivamente"). Notificacoes pelo toast classico (Django messages).
- Criado o model `EnderecoIP` (categoria, endereco IP unico, nome, fabricante, MAC, acesso, observacoes) na migration `0016`, registrado no admin. O seed inicial vem da migration de dados `0017`, que le um arquivo LOCAL ignorado pelo Git (`seed/ips_seed.json`) com os 101 IPs do banco antigo (19 servidores, 10 switches, 20 catracas/IdFace, 20 impressoras, 30 Wi-Fi, 2 monitoramento) — os MACs e credenciais de acesso NAO ficam versionados; em um clone limpo (sem o arquivo) a migration nao cadastra nada. Idempotente e com dedupe de IP no seed.
- Rotas criadas (todas restritas a TI/admin no backend; usuario comum e redirecionado): `GET /ips/`, `POST /ips/criar/`, `POST /ips/<id>/editar/`, `POST /ips/<id>/excluir/`. O `endereco_ip` e unico e a duplicidade e bloqueada no backend (na edicao, ignorando o proprio registro). Novos arquivos `static/css/ips.css`, `static/js/ips.js`, `templates/chamados/ips.html` e `partials/ip_modal.html`; adicionado o item "IPs" no menu lateral da TI. Testes automatizados (`core/tests.py`: `EnderecoIPTests`) cobrindo CRUD, unicidade do IP, categoria valida e permissoes.
- Adicionado o modulo "Licencas" (menu lateral, apenas TI/admin) para controle de licencas de software, migrando os dados do sistema antigo. A tela tem cartoes de resumo (Softwares, Quantidade contratada, Licencas cadastradas, Com prazo), um campo de pesquisa dinamica (client-side, filtra por software, serial, usuario, e-mail ou pagamento) e a lista de softwares em cards expansiveis (colapsam/expandem ao clicar no titulo). Cada card mostra o nome, um badge `cadastradas/contratadas` (verde quando completo), as observacoes e uma tabela responsiva (que vira lista de cards no celular) com as licencas: usuario, e-mail vinculado (link `mailto:`), serial (fonte mono), prazo (pill; "Indeterminado" ou a data), forma de pagamento (com "cartao final XXXX") e acoes. Botoes "Novo software" e "Nova licenca" no topo, "+ Adicionar licenca" e "Editar software" dentro de cada card, e "Editar" por licenca. Todo o cadastro/edicao acontece em modais Bootstrap reutilizados (create/edit), e a exclusao (de software ou licenca) pede confirmacao inline dentro do modal antes de enviar ("Excluir definitivamente"); excluir um software apaga tambem suas licencas em cascata. Ao escolher o prazo "Prazo para expirar", aparece o campo de data; em "Indeterminado" a data e ignorada.
- Criados os models `LicencaSoftware` (nome, quantidade contratada, observacoes) e `Licenca` (software, serial, e-mail vinculado, tipo/data de expiracao, forma de pagamento, final do cartao, usuario atribuido, observacoes) na migration `0014`, registrados no admin (com inline de licencas no software). Um software 1--N licencas (`on_delete=CASCADE`). O seed inicial vem da migration de dados `0015`, que le um arquivo LOCAL ignorado pelo Git (`seed/licencas_seed.json`) com os 3 softwares (AutoCAD 2014, Gestarcad 2026, Project 2021 STD) e as 14 licencas exportadas do banco antigo — os seriais/product keys e nomes de colaboradores NAO ficam versionados; em um clone limpo (sem o arquivo) a migration nao cadastra nada. Idempotente (nao duplica se ja houver softwares).
- Rotas criadas (todas restritas a TI/admin no backend; usuario comum e redirecionado): `GET /licencas/`, `POST /licencas/softwares/criar/`, `POST /licencas/softwares/<id>/editar/`, `POST /licencas/softwares/<id>/excluir/`, `POST /licencas/criar/`, `POST /licencas/<id>/editar/`, `POST /licencas/<id>/excluir/`. Novos arquivos `static/css/licencas.css`, `static/js/licencas.js`, `templates/chamados/licencas.html` e os modais `partials/software_modal.html` e `partials/licenca_modal.html`; adicionado o item "Licencas" no menu lateral da TI (`partials/ti_sidebar.html`). Testes automatizados (`core/tests.py`: `LicencaTests`) cobrindo CRUD de software e licenca, cascata, regra de prazo indeterminado, validacoes e permissoes.

## 2026-07-13

- Modulo Ramais: adicionadas edicao e exclusao, ordenacao por cabecalho e e-mail como campo livre. Clicar em uma linha abre o modal "Editar ramal" (pre-preenchido) com botao "Salvar alteracoes" e "Excluir" (com confirmacao inline "Excluir definitivamente"); ambos so TI/admin, com toast classico. Os cabecalhos da tabela (Colaborador, Setor, Telefone, Ramal, E-mail) passaram a ser clicaveis para ordenar a lista, alternando crescente/decrescente (o Ramal ordena numericamente), com indicador de seta. No cadastro e na edicao, o e-mail virou um campo livre (pode-se digitar) mantendo o `select` que puxa as contas ja cadastradas — ao escolher uma conta, o e-mail e o colaborador sao preenchidos; ao digitar o e-mail manualmente, a vinculacao com a conta e desfeita. Novas rotas `POST /ramais/<id>/editar/` e `POST /ramais/<id>/excluir/` (TI/admin). Testes adicionados a `RamalCreateTests` (edicao, exclusao, e-mail livre e permissoes).
- Adicionado o modulo "Ramais" (menu lateral, apenas TI/admin): lista telefonica interna com colaborador, setor, telefone, ramal e e-mail. A tela tem card de resumo (total), campo de pesquisa dinamica (client-side, filtra por qualquer um dos campos enquanto digita) e uma tabela responsiva (que vira lista de cards no celular), com o ramal em destaque e o e-mail como link `mailto:`. O botao "Cadastrar novo" abre um modal com todos os campos; o campo de e-mail e um `select` que puxa as contas ja cadastradas no modulo Emails (`ContaEmail`) e, ao escolher uma, preenche automaticamente o nome do colaborador. O cadastro (`POST /ramais/criar/`) resolve o e-mail a partir da conta escolhida, vincula a `ContaEmail`, notifica pelo toast classico (Django messages) e redireciona. Criado o model `Ramal` (migration `0012`) e uma migration de dados (`0013`) que faz o seed inicial lendo um arquivo LOCAL ignorado pelo Git (`seed/ramais_seed.json`) — os dados pessoais (nomes, telefones e e-mails) NAO ficam versionados no repositorio; no ambiente local ja estao cadastrados os 88 contatos do PDF `lista_contatos_sidertec`, cada ramal vinculado a sua `ContaEmail` por e-mail quando ela existir. Em um clone limpo (sem o arquivo de seed) a migration simplesmente nao cadastra nada. Registrado no admin; rotas `/ramais/` e `/ramais/criar/`, restritas a TI/admin no backend. Novos `static/css/ramais.css` e `static/js/ramais.js`. Testes `RamalCreateTests` (cadastro com e-mail selecionado + permissoes).
- Adicionado o modulo "Emails" (menu lateral, apenas TI/admin) para consulta e atualizacao das contas de e-mail corporativo. A tela mostra cards de resumo (total, ativas, suspensas), um campo de pesquisa dinamica (client-side, filtra por nome, e-mail, departamento, cargo, telefone e status) e uma tabela responsiva (que vira lista de cards no celular); clicar em uma linha abre um modal com todos os dados da conta agrupados (identificacao, organizacao, contato, seguranca/acesso e armazenamento). O botao "Importar lista" abre um modal que envia o CSV exportado do Google Workspace; a importacao faz upsert pela chave e-mail (atualiza os existentes e cria os novos), notifica pelo toast classico (Django messages) com a contagem de novas/atualizadas e redireciona para a listagem. A senha presente no export nunca e armazenada. Criado o model `ContaEmail` (migration `0011`), registrado no admin; rotas `/emails/`, `/emails/importar/` e `/emails/<id>/`, todas restritas a TI/admin no backend. Novos arquivos `static/css/emails.css` e `static/js/emails.js`. Adicionado `User_Download_*.csv` ao `.gitignore` (a lista contem dados pessoais e nao deve ser versionada). Testes automatizados (`core/tests.py`: `ContaEmailImportTests`) cobrindo import/upsert e permissoes.
- Revertida a quebra de colunas em linhas no Kanban (nao ficou bom): o quadro voltou ao formato de colunas lado a lado com rolagem horizontal dentro do quadro (padrao anterior). Corrigido o vazamento dos itens do card para a direita: as linhas rotulo/valor (`.ticket-meta`) passaram a usar `grid-template-columns: minmax(0, 1fr)` e `min-width: 0`, para o valor (`dd`) encolher com reticencias em vez de estourar o card; a largura minima das colunas foi restaurada para 300px (basis 320px), assim os valores comuns cabem sem cortar.
- Ajuste fino do Kanban: eliminada a barra de rolagem vertical da propria sidebar em telas de laptop mais baixas (ex.: 1366x768, 1280x720). No desktop (>=992px) a sidebar deixou de rolar como um todo (`overflow: hidden`); quando falta altura, apenas o menu rola internamente (`.sidebar-menu` com `flex: 1; overflow-y: auto`), mantendo a marca no topo e o rodape (usuario logado + botao Sair) sempre visiveis; em janelas baixas (`max-height: 820px`) o texto descritivo e ocultado para liberar altura. Corrigida tambem a barra de rolagem horizontal que surgia dentro de cada coluna: a lista de cards (`.ticket-list`) passou a usar `grid-template-columns: minmax(0, 1fr)` (antes a coluna implicita do grid era dimensionada pelo conteudo do card, estourando a largura) e os cards receberam `min-width: 0`. Largura minima das colunas reduzida de 300px para 250px. Observacao: a rolagem HORIZONTAL do quadro (`.kanban-wrapper`) continua quando ha mais colunas do que cabem na largura (fila + pendencias + N atendentes) — isso e o comportamento padrao de um Kanban e a rolagem ocorre dentro do quadro, nao na pagina.
- Tornado o sistema responsivo em todos os tamanhos de tela, eliminando as barras de rolagem indesejadas. Abaixo de 992px o menu lateral deixa de empilhar sua altura inteira no topo (o que empurrava o conteudo para fora da tela) e vira uma barra superior fina com o menu recolhido atras de um botao hamburger (novo `static/js/sidebar.js`), abrindo/fechando sem recarregar e fechando ao escolher um item; de 992px para cima nada muda (sidebar lateral de 290px). Criado `static/css/base.css` (carregado em `base.html`) com: rede de seguranca contra rolagem horizontal da pagina (`overflow-x: clip`, que nao quebra o `position: sticky` da sidebar), `max-width` em imagens/svg e barras de rolagem finas e discretas nas areas que realmente rolam (colunas do Kanban, corpos de modal, tabelas). O Kanban de altura cheia (100vh) passou a valer a partir de 992px, acompanhando a sidebar lateral. Corrigido overflow horizontal do modulo Insumos em telas estreitas (`min-width: 0` na secao permite que a tabela role dentro do card em vez de estourar a pagina) e blindados os grids `auto-fill/auto-fit` com `minmax(min(Npx, 100%), 1fr)` (Insumos, Requisicoes, modal de fechados). Ajustado o `grid-template-rows: auto 1fr` do shell no mobile para a barra escura nao esticar em paginas de conteudo curto. Sem alteracao de model, rota ou logica de negocio (apenas apresentacao). Verificado com capturas automatizadas (Playwright) em 390/768/1024/1440px: nenhuma pagina apresenta rolagem horizontal e as telas cabem na altura da janela (exceto rolagem natural de conteudo longo).
- Resolvida a pendencia de arquivos orfaos do modulo Requisicoes: ao excluir uma requisicao (ou um orcamento/suborcamento/documento isolado), os arquivos fisicos correspondentes em `MEDIA_ROOT/contratos/...` (fotos de produto e documentos anexos) agora sao removidos do disco. Implementado via signals `post_delete` em `core/signals.py` (registrados no `CoreConfig.ready()`), conectados a `OrcamentoContrato`, `OrcamentoDocumento`, `SuborcamentoContrato` e `SuborcamentoDocumento`; o cascade (`on_delete=CASCADE`) dispara o `post_delete` de cada registro apagado e cada arquivo (`FileField`/`ImageField`) e removido com `.delete(save=False)`. Diretorios que ficam vazios sao removidos, subindo ate o `MEDIA_ROOT` sem nunca apaga-lo. Sem alteracao de model (nenhuma migration). Teste automatizado adicionado (`core/tests.py`: `RequisicaoDeleteFilesTests`) confirmando que fotos e documentos de orcamento/suborcamento somem do disco apos a exclusao.

## 2026-07-02

- Corrigida a visualizacao das fotos dos equipamentos no modulo Emprestimos: as fotos agora aparecem como miniaturas (proporcao mantida com `object-fit: cover`, cantos arredondados, sombra e hover) e o clique na miniatura abre a imagem ampliada em um lightbox (fundo escurecido, imagem centralizada que se ajusta a tela, botao fechar e tecla Esc), alem de um link discreto "abrir original". Arquivos que nao sao imagem (extensao diferente de jpg/jpeg/png/webp/gif) continuam como link/icone de documento. Causa: o detalhe usava a URL crua de `MEDIA`, que nao e servida com `DEBUG=False` (retornava 404, exibindo icone de imagem quebrada); as fotos passaram a ser servidas por rota protegida `GET /emprestimos/fotos/<id>/` (TI/admin; usuario comum recebe 404), no mesmo padrao dos demais modulos. O termo assinado tambem passou a ser servido por rota protegida (`/emprestimos/<id>/termo-assinado/`), corrigindo o mesmo problema no link de abrir o termo assinado. Ajuste visual/serving, sem alteracao de model nem migration.
- Adicionado o modulo "Emprestimos" (menu lateral, apenas TI/admin) para controle de comodato de equipamentos de TI. A tela lista os emprestimos (colaborador, empresa, equipamento principal, quantidade, datas e status) e clicar abre um modal com todos os detalhes, equipamentos, fotos, termo e documentacao. O cadastro (modal) preenche os dados do colaborador uma vez e permite adicionar 1..N equipamentos em blocos dinamicos ("Adicionar mais um equipamento"), cada um com fotos; previsao de devolucao em branco = prazo indeterminado.
- Criados os models `EmprestimoTI`, `EquipamentoEmprestimoTI`, `FotoEquipamentoEmprestimoTI`, `AssinaturaResponsavelTI` e `LogUsoAssinaturaTI` (migration `0010`), registrados no admin. A assinatura do responsavel de TI e cadastrada com nome, imagem e senha; a senha e guardada com hash Django (nunca em texto puro) e conferida na criacao do emprestimo — senha correta aplica a assinatura no termo e registra o uso em `LogUsoAssinaturaTI`; senha errada bloqueia (`403`).
- O termo de responsabilidade e gerado automaticamente em PDF com ReportLab (sem servico externo), espelhando o modelo institucional da Sidertec (cabecalho / Departamento de TI, titulo, dados do colaborador, equipamentos como Equipamento 1..N, condicoes, responsabilidades, assinaturas do colaborador e do responsavel TI, rubrica e data de geracao). O termo fica salvo em `termo_pdf` e pode ser baixado. Se a previsao de devolucao estiver em branco, o termo mostra "Indeterminada".
- Fluxo de documentacao: apos o cadastro o status inicial e "Aguardando documentacao assinada"; e possivel anexar o termo assinado devolvido (registrando data e usuario) e marcar como OK, mudando o status para "Documentacao assinada / OK" (exige o termo assinado anexado, senao `409`). Status: Aguardando documentacao assinada, Documentacao assinada / OK, Em andamento, Devolvido, Cancelado. Rotas sob `/emprestimos/` (criar, detalhe, baixar-termo, anexar-termo-assinado, marcar-documentacao-ok, assinaturas/criar), todas restritas a TI/admin no backend. Adicionado `reportlab` ao `requirements.txt`.
- Adicionado o modulo "Documentos" (menu lateral, apenas TI/admin) para cadastro e armazenamento de documentos internos. A tela lista os documentos (nome, observacao resumida, quantidade de anexos, data e quem cadastrou); o botao "+ Adicionar" abre um modal de cadastro (nome, observacao e anexos multiplos, `multipart/form-data`, sem restricao de tipo/tamanho no codigo) e clicar em um item abre um modal de detalhe com a observacao completa e os anexos (link para abrir/baixar). Tudo por `fetch`/`JsonResponse` com CSRF, sem refresh.
- Criados os models `DocumentoTI` (nome, observacao, ativo, criado_por, datas) e `DocumentoTIAnexo` (documento, arquivo, nome_original, enviado_por, enviado_em), migration `0009`, registrados no admin com `list_display` util e inline de anexos. Rotas: `/documentos/`, `/documentos/criar/`, `/documentos/<id>/`, `/documentos/anexos/<id>/`, todas restritas a TI/admin no backend (comum recebe `403` nos endpoints e `404` nos anexos). Os arquivos ficam em `MEDIA_ROOT/documentos/<id>/` e sao servidos por rota protegida. Documentos/anexos nao sao apagados automaticamente (campo `ativo` preparado).
- Adicionado o modulo "Insumos" (menu lateral, apenas TI/admin) para controle simples de estoque e retirada de materiais de TI. A tela tem duas areas: "Estoque de insumos" (cards com nome, descricao, quantidade e status Disponivel/Baixo estoque/Sem estoque, com botao "Retirar") e "Ultimas retiradas" (tabela com insumo, quantidade, entregue para, motivo, quem registrou e data/hora). Botao "+ Adicionar" abre modal de cadastro; tudo por `fetch`/`JsonResponse` com CSRF, sem refresh.
- Criados os models `InsumoTI` (nome, descricao, quantidade_atual, observacao, ativo, criado_por, datas) e `RetiradaInsumoTI` (insumo, quantidade, entregue_para, motivo, registrado_por, criado_em), migration `0008`, registrados no admin com `list_display` util e inline de retiradas.
- Regras: cadastro exige quantidade inicial obrigatoria e nao negativa; a retirada valida no backend quantidade > 0 (bloqueia zero/negativo) e menor/igual ao estoque (`409` se insuficiente), com `entregue_para` e `motivo` obrigatorios. A baixa de estoque e o registro do historico ocorrem em uma transacao (`select_for_update`); o card (quantidade + status) e a tabela de retiradas sao atualizados sem refresh. Historico de retiradas nao e apagado e insumos nao sao excluidos automaticamente (campo `ativo` preparado). Rotas: `/insumos/`, `/insumos/criar/`, `/insumos/<id>/retirar/`, todas restritas a TI/admin no backend (comum recebe `403`).
- Adicionada a exclusao de requisicao no modulo Requisicoes. O modal de detalhe passou a ter um botao "Excluir" discreto (rodape) que abre uma confirmacao obrigatoria ("Tem certeza que deseja excluir esta requisicao? Esta acao nao podera ser desfeita." + aviso de que remove orcamentos, suborcamentos e documentos), com "Cancelar" e "Excluir definitivamente" (estilo perigoso). Cancelar nao faz nada; confirmar envia `POST /contratos/requisicoes/<id>/excluir/` com CSRF.
- O endpoint de exclusao valida permissao TI/admin no backend (usuario comum recebe `403`), aceita apenas `POST` (GET retorna `405`) e remove a requisicao com seus orcamentos, suborcamentos e documentos por cascata (`on_delete=CASCADE`). Em caso de sucesso o item some da lista sem refresh; em erro permanece visivel com mensagem. Sem alteracao de model (nenhuma migration criada). Pendencia registrada: os arquivos fisicos em `MEDIA_ROOT` nao sao removidos do disco (sem rotina de limpeza de orfaos ainda).
- Renomeado o modulo na interface de "Contratos" para "Requisicoes": menu lateral, `<title>`, card superior (titulo "Requisicoes", subtitulo "Cadastro de requisicoes" e descricao curta) e kicker do modal de criacao. Removidas as referencias visuais a "Contratos"/"Requisicoes de contrato". Os nomes tecnicos internos (models `*Contrato`, rotas `/contratos/...`, arquivos `contratos.css`/`contratos.js` e classes CSS) foram mantidos para nao gerar migration nem risco de quebra; nenhuma migration foi criada (mudanca apenas visual/textual).
- Melhorado o botao de nova requisicao: virou uma pill "+ Adicionar" (icone + rotulo) com hover/transicao; responsivo, mostrando apenas "+" em telas menores (<=640px). Abre o mesmo modal de cadastro.
- Compactado o card superior da tela de Requisicoes: menos padding e margens, titulo menor, e alinhamento de titulo/subtitulo/botao na mesma linha em telas maiores (com quebra responsiva em telas menores), liberando area util para a lista.
- Adicionado o modulo "Contratos" (menu lateral, apenas TI/admin). Tela principal lista as requisicoes (titulo + status) com botao "+" para cadastro em modal; clicar em uma requisicao abre um modal com todos os dados e seus orcamentos, cada um com os suborcamentos indentados abaixo e os totais destacados. Toda a interacao e por `fetch`/`JsonResponse` com CSRF, sem refresh.
- Criados os models `RequisicaoContrato`, `OrcamentoContrato`, `OrcamentoDocumento`, `SuborcamentoContrato` e `SuborcamentoDocumento` (migration `0007`). Requisicao 1--N orcamento 1--N suborcamento; orcamentos e suborcamentos tem 1--N documentos. Requisicao nasce com status "Aberta" e `criado_por` = usuario logado.
- Regra de calculo: total do orcamento = valor x quantidade + frete - desconto; o total exibido considera tambem a soma dos totais dos suborcamentos. O backend valida moeda (BRL/USD), exige quantidade >= 1 e bloqueia valores negativos em valor/frete/desconto.
- Orcamento e suborcamento aceitam foto do produto (imagem) e multiplos documentos anexos (sem restricao de tipo/tamanho no codigo), via `multipart/form-data`. Botao "Tirar print" captura a tela pelo navegador (`getDisplayMedia`), recorta uma regiao no canvas, pre-visualiza e anexa o recorte como foto do produto; com navegador sem suporte, orienta anexar imagem manualmente e nao trava se a captura for cancelada.
- Rotas criadas: `/contratos/`, `/contratos/requisicoes/criar/`, `/contratos/requisicoes/<id>/`, `/contratos/requisicoes/<id>/orcamentos/criar/`, `/contratos/orcamentos/<id>/suborcamentos/criar/`, alem de rotas protegidas para servir fotos e baixar documentos. Todas validam permissao TI/admin no backend (usuario comum: `403` nos endpoints e `404` nos arquivos). Models registrados no `admin.py` com `list_display` util e inlines. Adicionado `Pillow` ao `requirements.txt` (dependencia do `ImageField`).
- O fechamento de chamado deixou de ser possivel por drag: a coluna "Chamados fechados" nao aceita mais o drop direto. No frontend, ao soltar um card nessa coluna o movimento e cancelado (o card volta para a coluna de origem) com a mensagem "Para fechar o chamado, inicie o atendimento e finalize usando o botao Stop."; no backend, o endpoint `POST /chamados/mover/` recusa o destino `fechado` com `409` (nao aceita mais `target=fechado`). A coluna passou a ser apenas lista/consulta e so recebe chamados via Stop.
- O fechamento agora acontece exclusivamente pela acao "Stop": o backend (`POST /chamados/atendimento/encerrar/`) exige Atendente TI/Admin (`403` para usuario comum), atendimento ativo/Play em andamento do proprio usuario (`409` se nao houver), o campo "O que foi feito" preenchido e chamado ainda nao encerrado. Ao finalizar, salva o status "Fechado", `fechado_em`, atendente = quem finalizou, move o card para "Chamados fechados" e atualiza o badge (texto/cor) sem refresh. O botao de envio do modal passou a se chamar "Finalizar chamado".
- O historico tecnico do encerramento passou a registrar quem finalizou e o texto de "O que foi feito" (ex.: "Chamado finalizado por fabiano.polone. O que foi feito: atualizacao do driver e validacao com o usuario."), alem da mudanca de status, sem duplicar eventos e sem misturar com a conversa do usuario (`ChamadoMensagem`). Sem alteracao de model/banco (sem migration). Testes automatizados atualizados/adicionados (`core/tests.py`): fechamento via Stop registra o texto, drag para "fechado" e bloqueado e Stop exige Play ativo.
- Corrigida a regra do "Play" (iniciar atendimento) no Kanban: o botao passou a existir e funcionar somente para chamados em uma coluna de Atendente TI. Cards em "Chamados abertos" e "Chamados fechados" nao mostram mais o painel Play/Pause/Stop (regra em CSS que reage ao `data-column-type` da coluna, entao ao arrastar o card entre colunas o botao aparece/some sem refresh e sem alterar o drag-and-drop). Pendencias seguem sem Play (card proprio, sem o painel).
- O endpoint `POST /chamados/atendimento/iniciar/` passou a validar no backend: exige permissao de Atendente TI/Admin (usuario comum recebe `403`) e bloqueia com `409` o Play em chamado aberto sem atendente atual e em chamado encerrado (resolvido/fechado), mesmo se chamado direto pela URL. A regra "coluna de atendente" espelha o quadro (nao encerrado e `atendente_atual` no grupo `Atendente TI`). O `AtendimentoHistorico` so e criado quando o Play e valido; acoes bloqueadas nao geram historico e a regra de um unico atendimento ativo por atendente evita duplicidade. Sem alteracao de model/banco (sem migration).
- O titulo "Chamados fechados" no Kanban ficou clicavel (hover discreto com icone de lupa) e abre um modal moderno e responsivo, com animacao de entrada, para consultar os chamados encerrados sem sair da tela. O drag-and-drop, a criacao de chamados/pendencias e as demais colunas nao foram alterados.
- O modal tem dois estados: (1) barra de pesquisa inteligente + lista compacta (ID e titulo) e (2) detalhe completo do chamado. A lista inicial mostra os encerrados mais recentes; clicar em um item abre o detalhe (numero, titulo, descricao, solicitante, atendente, status, data de criacao, data de fechamento, anexos, conversa e historico tecnico recolhido). Botoes "Voltar" (retorna a lista) e "Fechar".
- A pesquisa e dinamica (enquanto digita), com debounce de ~300ms e cancelamento das requisicoes anteriores (AbortController); e case-insensitive, aceita partes do texto e filtra por ID/numero, titulo, descricao, solicitante, atendente atual, mensagens da conversa e descricao dos eventos do historico. Sem resultados exibe "Nenhum chamado fechado encontrado."; a busca usa `distinct()` e limita a 100 registros para nao pesar.
- Criadas as rotas `GET /chamados/fechados/buscar/` (lista/pesquisa em JSON) e `GET /chamados/fechados/<numero>/` (detalhe completo em JSON), ambas restritas a Atendente TI/Admin (usuario comum recebe `403`, validado no backend). O detalhe so expoe chamados encerrados (senao `404`) e reaproveita as rotas protegidas para download de anexos. Nao houve alteracao de model (sem migration).
- Corrigida a altura responsiva das colunas do Kanban para evitar rolagem interna antes de ocupar a altura disponivel da tela. A causa era o `max-height: calc(100vh - 5rem)` fixo (estimativa que rolava cedo e deixava espaco embaixo). Substituido por uma cadeia flex de altura no desktop (>=1200px): a tela do Kanban usa `100vh`, a area do Kanban recebe `flex: 1`, as colunas usam `height: 100%` e apenas a lista de cards rola (`overflow-y: auto` com `min-height: 0`). Assim a rolagem interna so aparece quando os cards realmente ultrapassam a coluna; em telas menores/empilhadas mantem o comportamento natural.
- Removido o card/header superior da tela "Chamados" (titulo, descricao e botao "Criar chamado"), liberando mais altura vertical para o Kanban (colunas passaram a usar `max-height: calc(100vh - 5rem)`).
- A criacao de chamado passou para um botao "+" redondo no topo da coluna "Chamados abertos", que abre o mesmo modal de criacao (titulo, descricao e anexos). Reutiliza a rota `POST /chamados/criar/` (apenas TI/admin, CSRF): chamado com solicitante = usuario logado, status "Aberto", sem atendente atual, entrando na coluna "Chamados abertos" sem refresh e registrando "Chamado criado manualmente pelo atendente X." no historico. Botao "+" padronizado com a coluna "Pendencias" (classe `column-add-btn`).
- Corrigido o texto de coluna vazia da coluna "Pendencias": a mensagem "Nenhuma pendencia cadastrada." deixava de sumir ao criar uma pendencia porque a funcao de recalculo do estado vazio so varria as colunas de chamados (`.js-ticket-list`), ignorando a coluna de pendencias (`.js-pendencia-list`). A funcao passou a cobrir tambem a coluna de pendencias, entao o texto some ao criar e reaparece apenas quando a ultima pendencia sai da coluna (ex.: convertida em chamado), sem refresh.
- Adicionada a coluna "Pendencias" no Kanban (entre "Chamados abertos" e as colunas dos atendentes) com botao "+" para cadastro em modal, card compacto exibindo so o titulo e modal de detalhe (titulo, descricao, data e autor). Criado o model `PendenciaTI` e a migration `0006_pendenciati`.
- Arrastar uma pendencia para a coluna de um atendente a converte em chamado (titulo/descricao da pendencia, solicitante = criador, atendente atual = atendente destino, status "Em atendimento"); a pendencia sai da coluna, o novo card aparece sem refresh e o historico registra a criacao a partir da pendencia e a atribuicao ao atendente. Conversao idempotente (sem chamado duplicado) e restrita a TI/admin; usuario comum recebe 403.
- Criadas as rotas `POST /chamados/pendencias/criar/`, `GET /chamados/pendencias/<id>/` e `POST /chamados/pendencias/<id>/converter/`. Regras de drag: pendencia so vai para colunas de atendente; chamado normal nao entra na coluna de pendencias; destino invalido devolve o card.
- Ajuste responsivo dos cards e colunas do Kanban para melhor adaptacao ao tamanho do monitor: colunas passaram de largura fixa (teto de 340px) para flex fluido (crescem em telas grandes, encolhem ate um minimo legivel e rolam horizontalmente quando ha muitas); cada coluna usa a altura do navegador com scroll interno da lista de cards (sem scroll vertical desnecessario da pagina); cards com padding/espacamento fluidos (clamp), titulo limitado a 2 linhas com reticencias e solicitante/atendente com ellipsis para nomes longos. Drag-and-drop, clique no card, botao "Criar chamado" e mensagens de coluna vazia preservados.
- Reduzida a altura do header da tela "Chamados" (Quadro de Atendimento) para melhorar o espaco util do Kanban: header compacto (menos padding e margem), titulo "Quadro de Atendimento" menor, descricao em linha unica e botao "Criar chamado" alinhado a direita no mesmo card. Aplicado via modificador `tickets-topbar--compact`, sem afetar os headers das outras telas.
- Ajustado o encerramento pelo botao "Stop": alem de finalizar o periodo de atendimento, o chamado agora e encerrado (status "Fechado", `fechado_em` preenchido, atendente atual = quem encerrou) e o card e movido automaticamente para a coluna "Chamados fechados" no Kanban, com badge atualizado sem refresh. Em caso de falha, o card permanece na coluna original.
- O encerramento passou a exigir permissao de Atendente TI/Admin (`/chamados/atendimento/encerrar/` retorna `403` para usuario comum) e registra no historico tecnico a mudanca de status e o evento "Chamado encerrado por X.", sem duplicar registros. O "Pause" continua apenas encerrando o periodo, sem mudar o status.
- Adicionada a area de conversa no detalhe do chamado, permitindo troca de mensagens entre solicitante e Atendente TI/Admin, com anexos opcionais por mensagem (multiplos arquivos, sem limite de tamanho/extensao).
- Criados os models `ChamadoMensagem` (texto da conversa) e `ChamadoMensagemAnexo` (anexos da mensagem) e a migration `0005_chamadomensagem_chamadomensagemanexo`.
- Criadas as rotas `POST /meus-chamados/<numero>/mensagens/` (envio, com permissao validada no backend) e `GET /meus-chamados/<numero>/mensagens/anexo/<id>/` (download protegido de anexo de mensagem).
- Cada mensagem gera um evento resumido no historico tecnico (`ChamadoEvento` tipo `comentario`), sem duplicar o texto; a conversa guarda o conteudo e o historico guarda so o resumo.
- O historico tecnico do chamado passou a aparecer recolhido por padrao (elemento `details/summary`), expandindo e recolhendo ao clique, sem trocar de tela nem remover registros.
- Mensagens do solicitante e da equipe de TI sao diferenciadas visualmente; apos enviar, o usuario permanece no detalhe com notificacao de sucesso ou erro.
- Adicionados testes automatizados (`core/tests.py`) para envio de mensagem, anexos, resumo no historico e permissoes de acesso.
- Sincronizada a documentacao com o estado real do projeto: `01_arquitetura.md` (Kanban com dados reais, movimentacao persistida, `ti_required`, `static/js` e `sidebar.css`), `03_modelos.md` (quatro modelos: `Chamado`, `ChamadoEvento`, `ChamadoAnexo` e `AtendimentoHistorico`; `AnexoChamado` deixou de ser "previsto") e `00_visao_geral.md` (escopo atual e fora de escopo atualizados).
- Corrigido o texto de coluna vazia no Kanban apos movimentacao de cards: a mensagem de vazio (ex.: "Sem chamados em atendimento.") deixou de aparecer junto com os cards. O estado de vazio agora e recalculado na hora, no drag-and-drop, removendo a mensagem quando a coluna recebe um card e recriando-a (via `data-empty-text`) quando a coluna fica sem cards; em caso de falha no POST o card volta e a mensagem e recalculada.
- Corrigida a instabilidade vertical do menu lateral: os botoes mudavam de posicao ao trocar de tela (ex.: entre "Chamados" e "Permissoes") porque a `.tickets-sidebar` esticava ate a altura do conteudo e o `space-between` distribuia os blocos de forma diferente em cada pagina. A sidebar passou a ter altura fixa (`100vh`) e `position: sticky`, mantendo o menu sempre na mesma posicao.
- Centralizado o CSS do menu lateral em `static/css/sidebar.css` (fonte unica, carregada em `base.html`), removendo as regras duplicadas de `chamados.css` e a responsividade da sidebar do media query.
- Padronizado o visual do menu: espacamento interno consistente, bordas arredondadas, hover suave, estado ativo com destaque (barra de acento) e transicoes leves.
- Adicionados icones inline (SVG) aos itens do menu de TI e do portal, sem depender de biblioteca externa de icones.

## 2026-07-01

- Adicionado o botao "Criar chamado" na tela "Chamados" (Kanban) com modal responsivo, visivel apenas para TI/admin.
- Criado o endpoint `POST /chamados/criar/` (TI/admin, CSRF) que abre chamado com o atendente logado como solicitante, status "Aberto" e sem atendente atual.
- O card recem-criado e inserido na coluna "Chamados abertos" sem refresh; a criacao registra evento "Chamado criado manualmente pelo atendente X.".
- Padronizado o menu lateral da area de TI em um unico include reutilizavel (`partials/ti_sidebar.html`), eliminando a inconsistencia entre as telas de Kanban, Permissoes e Historico.
- Menu da area de TI passa a exibir apenas "Chamados" (Kanban) e "Permissoes"; removidos os itens antigos "Quadro de Atendimento", "Historico", "Chamados do Usuario" e "Relatorios".
- Padronizado o nome do botao principal para "Chamados" (apontando para o Kanban) em todas as telas.
- Reorganizado o Kanban para funcionar por fila e por atendente: coluna fixa "Chamados abertos", colunas dinamicas por Atendente TI e coluna fixa "Chamados fechados".
- Substituido o endpoint de status por `POST /chamados/mover/`, que recebe a coluna de destino (aberto/atendente/fechado) e valida o atendente de destino.
- Movimentacao entre colunas atualiza status e `atendente_atual`, registra o historico (puxar, transferir, fechar, devolver) e responde com dados para o badge atualizar sem refresh.
- Simplificado o menu do perfil Atendente TI para apenas "Chamados" e "Permissoes".
- Adicionado o campo `atendente_atual` ao `Chamado` (usuario que realizou a ultima acao; nao e dono do chamado).
- Criado o model `ChamadoEvento` como log de eventos do chamado (criacao, mudanca de status, troca de atendente).
- Ao mover um card no Kanban, o sistema salva status e atendente atual e registra os eventos correspondentes, sem duplicar registros.
- Encerramentos (`resolvido`/`fechado`) preenchem `fechado_em` e mantem o ultimo atendente que agiu.
- Corrigido o badge de status do Kanban para atualizar texto e cor imediatamente no drag (sem refresh).
- Exibidos no detalhe do chamado o atendente atual, o historico de eventos e os anexos (nome, download, data e usuario).
- Criada a rota protegida de download de anexo (dono do chamado ou TI/admin), sem expor a URL direta de MEDIA.
- Migrado o Kanban da TI para usar chamados reais do banco, agrupados por status, removendo todo o dado mockado/fixo.
- Removidos os 9 chamados-fixture do banco (e os registros de atendimento vinculados) apos backup.
- Definidas as colunas do Kanban pelos status do model: Aberto, Em atendimento, Aguardando, Resolvido e Fechado.
- Criado o endpoint `POST /chamados/status/atualizar/` para alterar o status via drag-and-drop, com login, permissao de TI e CSRF.
- Restringido o acesso ao Kanban a administrador e Atendente TI; usuario comum e redirecionado ao portal.
- Cards do Kanban agora exibem numero, titulo, solicitante, data de abertura e status; clique abre o detalhe.
- Simplificada a abertura de chamado pelo usuario comum para apenas titulo, descricao e anexos.
- Criado o modelo `ChamadoAnexo` para vincular arquivos ao chamado, sem restricao de tamanho ou extensao.
- Adicionado suporte a upload de multiplos arquivos com `multipart/form-data`.
- Configurados `MEDIA_URL` e `MEDIA_ROOT` e o servico de media em desenvolvimento.
- Criado o portal do solicitante (visao do usuario comum) com as telas de abertura, listagem e detalhe de chamados.
- Adicionado o vinculo `solicitante` no modelo `Chamado` para associar o chamado ao usuario que o abriu.
- Definidos choices de `status` e `prioridade` no `Chamado`, com status default `aberto` e campo `fechado_em`.
- Criado o gerador de numero sequencial unico `CH-000123` para chamados abertos pelo portal.
- Adicionado o helper de permissao `is_common_user`.
- Implementado o roteamento pos-login por perfil: TI vai para o Kanban e usuario comum para `/meus-chamados/`.
- Adicionada a validacao de acesso para que o usuario comum veja apenas os proprios chamados.
- Criado o CSS `portal.css` e o sidebar reutilizavel do portal do solicitante.

## 2026-06-30

- Criada a base inicial do projeto Django.
- Implementada a tela visual de login.
- Criada a rota `/login/`.
- Criado redirecionamento da raiz `/` para login.
- Integrada autenticacao Active Directory/LDAP usando backend customizado.
- Configurado carregamento de variaveis LDAP a partir do `.env`.
- Implementado o fluxo autenticado com redirecionamento para `/chamados/`.
- Criado painel inicial Kanban para Atendente TI com dados mockados.
- Adicionado drag-and-drop visual com SortableJS.
- Mantido `TODO` explicito para futura persistencia da movimentacao no backend.
- Adicionados grupos `Administrador` e `Atendente TI` para controle inicial de acesso.
- Criada a tela `/permissoes/` para administradores.
- Definido `fabiano.polone` como administrador principal automatico.
- Criado componente global de notificacoes toast com animacao, fechamento manual e auto-dismiss.
- Criado modal global de detalhes do chamado no Kanban com dados mockados.
- Organizado o carregamento seguro dos dados do modal via `json_script` no template.
- Criados os modelos `Chamado` e `AtendimentoHistorico` para iniciar a persistencia do tempo de atendimento.
- Implementados os endpoints JSON para iniciar, pausar e finalizar periodos de atendimento.
- Adicionados Play, Pause, Stop e contador visual de atendimento nos cards do Kanban.
- Definida regra de processo para sempre registrar alteracoes com commit descritivo e push ao remoto.
- Criada a tela `/historico/` com busca dinamica de atendimentos e recorte por permissao.

## Convencao de registro

- Registrar aqui mudancas relevantes de produto, arquitetura e comportamento.
- Preferir entradas curtas, objetivas e datadas.
